# stu-pytorch

一个用于学习 PyTorch 计算机视觉模型的验证码识别项目。项目按四个阶段推进：

1. **第一阶段：使用 DenseNet121 初尝 CNN 识别**——借助 ImageNet 预训练模型，从整张图片直接预测 4 个字符。
2. **第二阶段：实现一个简单 CNN**——使用 `nn.Conv2d` 等基础组件自己搭建网络，理解卷积网络的结构和训练过程。
3. **第三阶段：实现可变长度验证码识别**——使用 CNN 提取横向视觉特征，结合 BiLSTM 与 CTC 在无需字符切分的情况下识别 2～6 位验证码，理解序列建模与自动对齐。
4. **第四阶段：CNN + Transformer Encoder + CTC（已完成）**——在相同数据上学习自注意力序列建模，逐步替换 BiLSTM。

## 第一阶段：DenseNet121

当前第一阶段使用 ImageNet 预训练的 DenseNet121，从整张图片直接预测 4 个字符，
不进行字符切割。

### 当前模型

- 输入：RGB 图片，统一缩放为 `180 × 100`
- 配置字符集：`0-9` 和 `a-z`，共 36 类
- 输出：`4 × 36 = 144` 个分数
- 训练集：`data/train`，800 张
- 验证集：`data/test`，381 张
- 权重：`stage1_densenet121/checkpoints/model-36.pth`
- 整图准确率：4 个字符全部正确的图片数 ÷ 图片总数
- 单字符准确率：预测正确的字符位置数 ÷ 字符位置总数

当前数据文件名均为 4 个字符，例如 `25mb.png`。文件名（不含扩展名）就是监督
标签。当前数据实际只覆盖 `2345678abcdefgmnpwxy` 这 20 个字符，因此训练出的
权重不能视为可靠支持全部 36 类。数据目录中只能放符合该规则的图片文件。

## 环境准备

项目需要 Python 3.13 和 [uv](https://docs.astral.sh/uv/)：

以下命令都需要在项目根目录执行，因为数据和权重使用相对路径。

```bash
uv sync
```

项目使用 `uv.lock` 固定依赖版本。

macOS 运行 Tkinter GUI 还需要：

```bash
brew install python-tk@3.13
```

PyTorch 会自动选择可用设备，顺序为 CUDA、Apple MPS、CPU。默认首次训练会下载
DenseNet121 的 ImageNet 预训练权重，之后使用本机缓存；使用 `--no-pretrained` 时不会下载。

## 代码格式与检查

项目使用 Ruff 统一检查和格式化 Python 代码。Ruff 作为开发依赖记录在 `pyproject.toml`
和 `uv.lock` 中，执行 `uv sync` 时会一并安装。

```bash
# 检查代码问题
uv run ruff check .

# 自动修复可以安全修复的问题
uv run ruff check . --fix

# 格式化代码
uv run ruff format .

# 只检查格式，不修改文件
uv run ruff format --check .
```

提交代码前推荐运行：

```bash
uv run ruff check . && uv run ruff format --check .
```

当前启用了常见代码错误、未使用代码、import 排序、现代 Python 写法和 bug-prone
模式检查，目标 Python 版本为 3.13，默认行宽为 88。

## 训练

当前推荐基线：

```bash
uv run python -m stage1_densenet121.train --loss multi-label --lr 0.001 --backbone-lr 0.0003
```

训练 30 轮，每轮评估一次，同时输出整图准确率和单字符准确率。只将验证整图准确率
最高的权重保存到 `stage1_densenet121/checkpoints/model-36.pth`；整图准确率相同时保留
loss 更低的权重。

两个阶段共用的 `--epochs`、`--batch-size`、`--lr` 和 `--plot-curves` 由
`training_config.py` 中的 `TrainingConfig` 统一定义和校验。两个阶段可以分别设置默认值。
曲线绘制逻辑统一放在 `training_curves.py` 中。

可用参数：

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| `--epochs` | `30` | 训练轮数 |
| `--batch-size` | `5` | 每批图片数量 |
| `--loss` | `multi-label` | `multi-label` 或 `cross-entropy` |
| `--lr` | `0.001` | 分类层学习率；不设置主干学习率时也是全模型学习率 |
| `--backbone-lr` | 未设置 | 单独指定 DenseNet 特征主干学习率 |
| `--classifier-only` | 关闭 | 冻结 DenseNet 特征层，只训练分类层 |
| `--no-pretrained` | 关闭 | 不加载 ImageNet 预训练权重，从随机初始化开始训练 |
| `--plot-curves` | 关闭 | 实时显示训练集/验证集的 loss、整图准确率和单字符准确率曲线 |

示例：

```bash
# 只训练最后一层
uv run python -m stage1_densenet121.train --classifier-only --loss multi-label --lr 0.001

# 主干和分类层使用不同学习率
uv run python -m stage1_densenet121.train --loss multi-label --lr 0.001 --backbone-lr 0.0003

# 不使用 ImageNet 预训练权重，从随机初始化开始训练
uv run python -m stage1_densenet121.train --no-pretrained --loss multi-label --lr 0.001

# 实时显示训练曲线
uv run python -m stage1_densenet121.train --plot-curves --loss multi-label --lr 0.001
```

启用 `--plot-curves` 后会打开一个 Matplotlib 窗口，每轮结束后更新三张图：
训练集/验证集 loss、整图准确率和单字符准确率。训练结束后关闭图表窗口，程序才会退出。

使用推荐的分层学习率时，一次本机参考实验达到 `99.21%`，即 381 张验证图片中
正确 378 张。该结果仅反映当前数据集，不代表其他验证码来源的识别率。

## 预测

训练完成后运行：

```bash
uv run python -m stage1_densenet121.predict
```

程序会逐张打印预测值和真实标签，并显示对应图片。关闭当前图片窗口后才会继续
下一张。

## GUI

```bash
uv run python -m stage1_densenet121.main
```

点击“选择图片”，选择一张文件名为正确标签的图片，再点击“识别”。GUI 的累计
准确率依赖文件名标签；文件名不是 4 个合法字符时会抛出 `ValueError`。

## 冒烟测试

```bash
uv run python -m stage1_densenet121.test_smoke
```

该检查会验证数据读取、模型的 144 维输出，以及两种损失函数能否正常计算；不会
修改训练权重。

## 第二阶段：简单 CNN

第二阶段直接使用项目现有的 4 位验证码数据集，不下载 MNIST 或 CIFAR-10。模型支持通过
`--conv-blocks` 选择三或四个卷积块，并通过 `--bottleneck-channels` 在分类头前加入
`1×1` 卷积瓶颈。根据完整实验结果，第二阶段结项配置采用四卷积块、64 通道瓶颈和位置感知
共享分类头；代码保留面向初学者的中文注释，方便观察各层张量形状与结构优化过程。

先运行冒烟测试，确认数据、模型输出和反向传播正常：

```bash
uv run python -m stage2_simple_cnn.test_smoke
```

使用当前结项结构和训练策略复现 V1：

```bash
uv run python -m stage2_simple_cnn.train \
  --conv-blocks 4 \
  --bottleneck-channels 64 \
  --classifier-head position \
  --dropout 0 \
  --seed 2 \
  --epochs 40 \
  --batch-size 16 \
  --lr 0.001 \
  --augment \
  --evaluate-clean-train \
  --reduce-lr-on-plateau \
  --weight-decay 0.001 \
  --synthetic-train-dir data/synthetic_train \
  --synthetic-ratio 0.75 \
  --synthetic-validation-dir data/synthetic_validation
```

先按后文说明生成本地合成训练集和合成验证集，再运行该推荐命令。`--weight-decay` 大于 0 时训练器使用 AdamW；保持默认值 0 时继续使用历史 Adam 基线，
便于复现实验。多随机种子对照后，当前推荐的权重衰减系数为 `0.001`。`--dropout` 可在
`[0, 1)` 范围设置，设为 0 即关闭。标签平滑实验通过 `--label-smoothing 0.05` 启用；
默认值 0 保留原始交叉熵基线。当前位置分类头的多随机种子实验表明
`--reduce-lr-on-plateau` 可提高后期收敛精度，因此当前推荐启用。

`--evaluate-clean-train` 每轮会额外使用未增强训练图片并在 `model.eval()` 下计算干净训练集
指标，因此会增加训练时间。启用绘图时，训练曲线使用这组干净指标与验证曲线比较；终端仍会
同时保留开启增强和 Dropout 时的在线训练指标。

验证新增第三个卷积块时，保持数据增强和学习率调度关闭，只改变模型结构：

```bash
uv run python -m stage2_simple_cnn.train \
  --epochs 30 \
  --batch-size 16 \
  --lr 0.001
```

第三个卷积块把通道数从 `32` 增加到 `64`。随后只额外压缩特征图高度，使送入全连接层的
特征总数仍为 `8448`，避免因为全连接层参数量增加而干扰结构对比。

验证第四个卷积块时，保持三卷积块最佳配置中的数据增强和固定学习率不变：

```bash
uv run python -m stage2_simple_cnn.train \
  --conv-blocks 4 \
  --epochs 30 \
  --batch-size 16 \
  --lr 0.001 \
  --augment
```

第四个卷积块将通道数从 `64` 增加到 `128`，只沿高度方向继续池化。四卷积块结构送入
全连接层的特征数为 `128 × 3 × 22 = 8448`，与三卷积块结构的
`64 × 6 × 22 = 8448` 相同。这样新增参数只来自第四个卷积块，更适合进行深度消融比较。

压缩全连接层实验保持三卷积块和空间尺寸不变，在分类头前增加 `1×1` 卷积，将通道从
`64` 压缩到 `32`：

```bash
uv run python -m stage2_simple_cnn.train \
  --conv-blocks 3 \
  --bottleneck-channels 32 \
  --epochs 30 \
  --batch-size 16 \
  --lr 0.001 \
  --augment
```

瓶颈不会改变 `6×22` 的空间布局，全连接层输入从 `64 × 6 × 22 = 8448` 降为
`32 × 6 × 22 = 4224`。模型参数量从 `1,240,464` 降至 `634,352`，减少约 `48.9%`。
该结构实验达到 69.03% 整图准确率和 89.90% 单字符准确率，优于无瓶颈基线。

继续验证压缩边界时，将瓶颈通道从 `32` 降至 `16`，其他条件保持不变：

```bash
uv run python -m stage2_simple_cnn.train \
  --conv-blocks 3 \
  --bottleneck-channels 16 \
  --epochs 30 \
  --batch-size 16 \
  --lr 0.001 \
  --augment
```

16 通道结构的全连接层输入为 `16 × 6 × 22 = 2112`，模型参数量为 `329,152`，
相比无瓶颈基线减少约 `73.5%`。该结构达到 73.23% 整图准确率和 91.14% 单字符准确率，
是三卷积块实验中的最佳精度配置。

最后将瓶颈从 `16` 通道降至 `8`，确定继续压缩分类头时的性能边界：

```bash
uv run python -m stage2_simple_cnn.train \
  --conv-blocks 3 \
  --bottleneck-channels 8 \
  --epochs 30 \
  --batch-size 16 \
  --lr 0.001 \
  --augment
```

8 通道结构的全连接层输入为 `8 × 6 × 22 = 1056`，模型参数量为 `176,552`，相比无瓶颈
基线减少约 `85.8%`，单次实验达到 72.97% 整图准确率和 91.08% 单字符准确率。

训练时实时显示 loss、整图准确率和单字符准确率曲线：

```bash
uv run python -m stage2_simple_cnn.train --epochs 10 --batch-size 16 --lr 0.001 --plot-curves
```

对训练集启用轻量仿射增强（验证集不增强）：

```bash
uv run python -m stage2_simple_cnn.train --epochs 30 --batch-size 16 --lr 0.001 --augment
```

`--augment` 会随机执行不超过 `3°` 的旋转、轻微平移、`0.95~1.05` 倍缩放和小幅错切；
不会执行会破坏验证码字符顺序的翻转或大幅裁剪。

`--color-jitter` 只作用于训练集，随机执行幅度为 `0.15` 的亮度、对比度和饱和度变化，
以及幅度为 `0.02` 的轻量色相变化；多随机种子实验表明它会降低当前模型的平均准确率，
因此默认关闭。

合成混合训练引入前，使用位置感知共享分类头建立纯真实数据结构基线，并将瓶颈通道设为 64：

```bash
uv run python -m stage2_simple_cnn.train \
  --conv-blocks 4 \
  --bottleneck-channels 64 \
  --classifier-head position \
  --dropout 0 \
  --seed 0 \
  --epochs 40 \
  --batch-size 16 \
  --lr 0.001 \
  --augment \
  --evaluate-clean-train \
  --reduce-lr-on-plateau \
  --weight-decay 0.001
```

`position` 分类头把宽度为 22 的特征图等分成 4 个字符区域，分别在空间维度求平均，再让
四个位置共享同一个 `64→36` 线性分类器。输出形状仍为 `[B, 4, 36]`。该配置参数量为
108,644，比 175,144 参数的 `flatten` 基线减少约 38.0%，同时把瓶颈从 8 通道提高到
64 通道。实现使用等宽分段均值而不是 `AdaptiveAvgPool2d((1, 4))`，以兼容 MPS 对非整除
自适应池化的限制。当前推荐不启用 `--color-jitter` 或 `--label-smoothing`。

比较位置分类头的 16、32、64 通道时，可以同时开启干净训练集评估：

```bash
for channels in 16 32 64; do
  for seed in 0 1 2; do
    uv run python -m stage2_simple_cnn.train \
      --conv-blocks 4 \
      --bottleneck-channels "$channels" \
      --classifier-head position \
      --dropout 0.1 \
      --seed "$seed" \
      --epochs 30 \
      --batch-size 16 \
      --lr 0.001 \
      --augment \
      --evaluate-clean-train \
      --weight-decay 0.001
  done
done
```

这组实验一次只比较瓶颈通道数。若之后单独比较 Dropout，可使用 `--dropout 0` 关闭或传入
其他 `[0, 1)` 范围内的概率，不应与瓶颈通道实验同时改变。

根据验证 loss 自动降低学习率：

```bash
uv run python -m stage2_simple_cnn.train \
  --epochs 30 \
  --batch-size 16 \
  --lr 0.001 \
  --reduce-lr-on-plateau
```

启用 `--reduce-lr-on-plateau` 后，如果验证 loss 连续多轮没有改善，学习率会降低为原来的
`30%`，最低降至 `0.00001`。它没有提升早期三卷积块模型，但后续位置感知分类头的多 seed
实验确认它能够改善当前推荐结构，因此当前推荐训练命令启用该调度器。

第二阶段的共用参数默认值为：`--epochs 10`、`--batch-size 16`、`--lr 0.001`，
`--plot-curves` 默认关闭。第二阶段额外支持 `--bottleneck-channels`、`--classifier-head`、
`--dropout`、`--seed`、`--weight-decay`、`--label-smoothing`、`--color-jitter`、
`--evaluate-clean-train`、`--train-dir`、`--validation-dir`、`--synthetic-train-dir`、
`--synthetic-ratio` 和 `--synthetic-validation-dir`。分类头默认使用 `flatten`，Dropout 默认是
`0.1`，随机种子默认是 `0`，权重衰减、标签平滑和合成比例默认都是 `0`，颜色增强、干净训练
评估和合成验证默认关闭。权重衰减为 0 时
使用 Adam 以复现历史基线，大于 0 时使用 AdamW。启用曲线后，每轮结束都会刷新训练集和
验证集曲线；训练结束后，关闭 Matplotlib 窗口程序才会退出。

这是用于学习和结构对比的项目，第二阶段默认只使用统一权重文件
`stage2_simple_cnn/checkpoints/model.pth`。不同卷积块、瓶颈通道和 seed 的训练会直接覆盖
该文件，不为每组参数长期保留独立 `.pth`；实验结论以日志和 README 记录为准。只有明确
需要保留某次权重时才传入 `--model-path`。权重选择首先比较验证集整图准确率；整图准确率
相同时保存验证 loss 更低的轮次。模型输入形状为 `[B, 3, 100, 180]`，输出形状为
`[B, 4, 36]`。其中 `B` 是批量大小，
`4` 是字符位置，`36` 是每个位置的候选字符数量。训练和验证都会同时输出整图准确率与
单字符准确率。

## 独立测试集与混淆矩阵

`data/test` 已经反复用于模型结构、训练策略和最佳轮次选择，因此它是验证集，不能继续作为
最终测试集。项目为真正未见过的数据预留本地目录 `data/independent_test`；该目录和评估报告
默认不提交到 Git，避免后续训练过程无意中使用独立测试图片。

优先将真实采集且从未参与训练、模型选择或人工调参的图片放入该目录。文件名必须是 4 位真实
标签，例如 `2n6m.png`。真实图片应来自独立采集批次，并覆盖不同字体、颜色、干扰线、背景和
字符粘连情况；不要从现有 `data/train` 或 `data/test` 复制或变换图片来充当独立测试数据。

没有新的真实图片时，可使用项目提供的合成生成器创建固定测试集：

```bash
uv run python -m stage2_simple_cnn.generate_captchas \
  --count 400 \
  --seed 20250310
```

生成器默认输出 400 张 `180×100` 四位验证码，使用数据中实际出现的 20 个字符，并让每个位置的
字符分布保持均衡。图片随机组合常见拉丁字体、灰白背景、颜色、阴影、旋转、伸缩、干扰线、
彩色网格、噪点和轻微模糊。`manifest.json` 会记录随机种子、字体、每张图片的样式参数和
SHA-256，可用于确认测试集没有被改写。目标目录已有图片时生成器默认拒绝覆盖；只有明确重建时
才使用 `--replace-existing`。

合成测试集主要衡量模型面对独立字体和渲染扰动时的迁移能力，不能替代同一业务来源的真实新
数据。生成参数确认后应冻结该批图片，不能根据测试结果反复修改生成器或模型。

使用当前推荐模型结构执行一次最终评估：

```bash
uv run python -m stage2_simple_cnn.evaluate
```

评估器会先使用 SHA-256 检查独立测试图片是否与 `data/train`、`data/test` 存在完全相同的
文件；发现历史目录或重复图片时默认拒绝继续。评估成功后生成：

| 文件 | 内容 |
| --- | --- |
| `stage2_simple_cnn/evaluation_reports/metrics.json` | 整图、单字符、各位置准确率及重复检查结果 |
| `stage2_simple_cnn/evaluation_reports/confusion_matrix.csv` | 完整 36 类字符混淆矩阵 |
| `stage2_simple_cnn/evaluation_reports/confusion_matrix.png` | 仅显示实际出现类别的可视化混淆矩阵 |
| `stage2_simple_cnn/evaluation_reports/errors.csv` | 每张错误图片、真实值、预测值、错误位置和置信度 |

若只是验证评估脚本或复查历史验证集，可以显式运行：

```bash
uv run python -m stage2_simple_cnn.evaluate \
  --data-dir data/test \
  --allow-known-data \
  --output-dir stage2_simple_cnn/evaluation_reports/validation
```

该命令会输出醒目警告，其结果仍然是验证指标，不是独立测试准确率。独立测试原则上只在模型、
训练参数和停止规则全部锁定后执行一次；如果根据独立测试集错误继续调整模型，这组数据就已经
转化为新的验证集，需要另行收集下一组独立测试数据。

独立测试图片和生成清单默认只保留在本地，不提交仓库。默认自动扫描系统中的常见拉丁字体，
因此相同 seed 只保证在字体集合不变的环境中复现；跨机器严格复现时，应通过可重复的
`--font-path` 显式指定同一组字体文件。

75% 合成比例的结构、训练参数和停止规则锁定后，本机使用 seed `20250310` 生成并冻结了最终
独立测试集：400 张、每个字符在每个位置各出现 20 次，与 `data/train`、`data/test`、
`data/synthetic_train` 和 `data/synthetic_validation` 的 SHA-256 重复数均为 0。使用循环训练后
保留的 Seed 2 当前权重执行一次最终评估，结果为：

| 指标 | 最终独立测试结果 |
| --- | ---: |
| 整图准确率 | **383 / 400，95.75%** |
| 单字符准确率 | **1583 / 1600，98.94%** |
| 第 1～4 位准确率 | 98.75%、98.75%、99.25%、99.00% |
| 整图错误数 | 17 |

最多的字符混淆为 `n→m` 3 次，其余列出的单类混淆均只有 1 次。这批 seed `20250310` 数据及其
结果现已锁定；不能再根据这 17 张错误图片调整模型、训练参数或生成器，否则它将失去最终测试集
资格。该指标衡量同一生成器家族下的新 seed 与新渲染组合，仍不能替代真实业务来源的新采集数据。

本机此前使用 400 张、seed `20250308` 的合成数据进行迁移测试，每个字符在每个位置各出现 20 次，
与历史训练/验证图片的 SHA-256 重复数为 0。首次生成时发现 `SFGeorgianRounded.ttf` 被名称中的
`Georgia` 误判为常规拉丁字体，缺失字符被画成多条横向占位符，人眼无法辨认。生成器现已同时
按字体名称排除非拉丁字体，并要求目标字符的字形掩码全部不同；训练集和验证集均已重新生成。
修复后、引入混合训练之前的统一权重合成验证基线为：

| 指标 | 结果 |
| --- | ---: |
| 整图准确率 | 55 / 400，13.75% |
| 单字符准确率 | 825 / 1600，51.56% |
| 第 1～4 位准确率 | 51.25%、47.75%、53.25%、54.00% |

该结果仍远低于历史验证集的 97.38%，说明模型明显依赖原始验证码来源中的字体和渲染分布，对独立
字体、排版和干扰组合的迁移能力不足。由于后续训练方向已经参考了该结果，这 400 张图片现位于
`data/synthetic_validation`，只作为合成鲁棒性验证集，不视为最终独立测试集。最终配置锁定后，
已另用未参与调参的 seed `20250310` 生成并评估上述最终独立测试集。

## 混合真实与合成数据训练

本机使用 seed `20250309` 单独生成 4000 张合成训练图片：

```bash
uv run python -m stage2_simple_cnn.generate_captchas \
  --output-dir data/synthetic_train \
  --count 4000 \
  --seed 20250309
```

训练器支持在每个批次中按固定数量混合真实和合成样本。以 batch size 16 为例，比例 `0.25`
表示每批 12 张真实图片和 4 张合成图片，比例 `0.5` 表示各 8 张。每轮至少完整遍历一次 800 张
真实训练图片；合成池按需随机抽样，避免 4000 张合成图片完全压过真实数据。默认值仍为 0，未传
合成参数时会复现原来的纯真实数据训练流程。

Seed 0 的比例实验结果如下；合成指标取原始验证集最佳权重所在轮次：

| 合成比例 | 原始验证整图准确率 | 合成验证整图准确率 | 原始正确数 | 合成正确数 |
| ---: | ---: | ---: | ---: | ---: |
| 0% | **96.85%** | 13.75% | 369 / 381 | 55 / 400 |
| 25% | 95.80% | 87.75% | 365 / 381 | 351 / 400 |
| 50% | 95.01% | 93.50% | 362 / 381 | 374 / 400 |
| **75%** | **96.59%** | **95.25%** | 368 / 381 | 381 / 400 |

75% 配置进一步使用 Seed 0、1、2 完成复验：

| Seed | 最佳轮次 | 原始验证整图 | 原始单字符 | 合成验证整图 | 合成单字符 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 0 | 40 | 96.59% | 99.08% | 95.25% | 98.75% |
| 1 | 39 | 96.59% | 99.08% | 97.00% | 99.19% |
| 2 | 38 | 96.33% | 99.02% | 96.50% | 99.12% |
| 平均 | — | **96.50%** | **99.06%** | **96.25%** | **99.02%** |
| 样本标准差 | — | **0.15** | **0.03** | **0.90** | **0.24** |

三 seed 的原始验证整图准确率范围只有 0.26 个百分点，说明结果稳定。平均干净训练整图准确率为
99.71%，平均训练—原始验证差距为 3.21 个百分点。与不混合合成数据的三 seed 原始验证平均值
97.20% 相比，75% 配置下降 0.70 个百分点，但获得了 96.25% 的合成验证平均准确率。当前选择
**75% 合成比例**作为推荐方案，即每个 batch 固定使用 4 张真实图片和 12 张合成图片：

```bash
uv run python -m stage2_simple_cnn.train \
  --conv-blocks 4 \
  --bottleneck-channels 64 \
  --classifier-head position \
  --dropout 0 \
  --seed 2 \
  --epochs 40 \
  --batch-size 16 \
  --lr 0.001 \
  --augment \
  --evaluate-clean-train \
  --reduce-lr-on-plateau \
  --weight-decay 0.001 \
  --synthetic-train-dir data/synthetic_train \
  --synthetic-ratio 0.75 \
  --synthetic-validation-dir data/synthetic_validation
```

训练过程每轮同时输出原始验证集和合成验证集指标，但学习率调度、最佳轮次选择和权重保存仍然只
依据原始验证集。三个 seed 用于验证配置稳定性，不把 seed 本身作为继续挑选最高分的超参数；V1
按用户确认冻结循环训练后保留并完成最终测试的 Seed 2 权重。由于 75% 每轮有 200 个 batch，
计算量分别约为 50% 和 25% 配置的 2 倍和 3 倍，
该收益包含更多合成样本和更多优化步骤的共同作用，不应解释为纯粹的比例消融结果。

运行生成器、混合采样和评估模块自身的测试：

```bash
uv run python -m stage2_simple_cnn.test_generate_captchas
uv run python -m stage2_simple_cnn.test_data_loading
uv run python -m stage2_simple_cnn.test_evaluate
```

## 文件结构

| 路径 | 用途 |
| --- | --- |
| `datasets.py` | 两个阶段共用：从文件名构造标签并加载图片 |
| `training_config.py` | 两个阶段共用：训练参数类和命令行参数定义 |
| `training_curves.py` | 两个阶段共用：实时 loss、整图准确率和单字符准确率曲线 |
| `training_metrics.py` | 两个阶段共用：整图与单字符准确率统计 |
| `stage1_densenet121/models.py` | 第一阶段 DenseNet121 和 144 维分类层 |
| `stage1_densenet121/train.py` | 第一阶段训练、验证和最佳权重保存 |
| `stage1_densenet121/predict.py` | 第一阶段单张识别与测试集逐张预测 |
| `stage1_densenet121/main.py` | 第一阶段 Tkinter 桌面界面 |
| `stage1_densenet121/test_smoke.py` | 第一阶段最小可运行检查 |
| `stage1_densenet121/checkpoints/model-36.pth` | 第一阶段 DenseNet121 权重 |
| `stage2_simple_cnn/models.py` | 三/四卷积块 CNN，以及展平/位置感知分类头 |
| `stage2_simple_cnn/data_loading.py` | 真实与合成数据的固定比例批次采样器 |
| `stage2_simple_cnn/train.py` | 第二阶段混合训练、双验证和最佳权重保存 |
| `stage2_simple_cnn/generate_captchas.py` | 生成可复现的四位合成验证码和数据清单 |
| `stage2_simple_cnn/evaluate.py` | 独立测试、数据重复检查、混淆矩阵和错误报告 |
| `stage2_simple_cnn/test_smoke.py` | 第二阶段数据、前向传播和反向传播检查 |
| `stage2_simple_cnn/test_data_loading.py` | 固定比例混合批次采样检查 |
| `stage2_simple_cnn/test_generate_captchas.py` | 合成验证码生成、均衡标签和覆盖保护检查 |
| `stage2_simple_cnn/test_evaluate.py` | 独立测试统计与报告生成检查 |
| `data/train2`、`data/test2` | 旧算式验证码数据，当前训练不使用 |
| `checkpoints/model.pth` | 旧算式验证码权重，当前代码不使用 |

第二阶段的简单 CNN 位于 `stage2_simple_cnn`，复用根目录下的 `datasets.py` 数据加载逻辑。

`get_imgs.py` 是旧算式验证码的下载脚本，当前 36 类训练流程不使用。

## 已知限制

- 只能识别固定 4 个字符。
- 分类层保留 `0-9a-z` 共 36 个输出，但当前训练和测试数据只覆盖实际出现的
  `2345678abcdefgmnpwxy` 共 20 个字符；95.75% 最终测试准确率不代表其余 16 类已被验证。
- 标签必须来自图片文件名。
- 合成生成器不能替代同一业务来源的新真实验证码，最终上线指标仍需真实独立数据验证。
- GUI 每次识别都会重新加载模型，适合演示，不适合高吞吐服务。
- 第二阶段输入只执行 `ToTensor()`，尚未评估基于当前数据集统计量的归一化；ImageNet mean/std
  不一定适合从零训练的 Simple CNN。
- 仅应在自有或明确授权的验证码系统中使用。

## 第二阶段结构优化实验记录

为减少简单 CNN 全连接层参数并改善泛化能力，实验固定使用以下基础训练条件，每次只改变
一个模型结构或训练策略因素：

```bash
uv run python -m stage2_simple_cnn.train \
  --epochs 30 \
  --batch-size 16 \
  --lr 0.001 \
  --plot-curves
```

数据集固定为 800 张训练图片和 381 张验证图片。历史单次实验使用随机种子 `0`；新增
`--seed` 后可以使用多个种子复验。下表记录本机 MPS 实验中观察到的最佳指标；
“单字符准确率”取对应实验所有轮次中的最高值，
不一定与最佳整图准确率出现在同一轮。

| 实验结构 | 模型参数量 | 最佳整图准确率 | 最佳单字符准确率 |
| --- | ---: | ---: | ---: |
| 原始两层 CNN，特征图 `25×45` | 5,189,232 | 40.94% | 74.48% |
| `AdaptiveAvgPool2d((5, 15))` | 350,832 | 21.78% | 63.19% |
| `AvgPool2d(2, 2)`，无 BatchNorm/Dropout | 1,221,744 | 43.04% | 约 76.5% |
| `AvgPool2d(2, 2) + Dropout(0.3)` | 1,221,744 | 40.94% | 75.72% |
| `AvgPool2d(2, 2) + Dropout(0.1)` | 1,221,744 | 43.83% | 75.85% |
| `AvgPool2d(2, 2) + BatchNorm`，无 Dropout | 1,221,840 | 44.88% | 77.69% |
| `AvgPool2d(2, 2) + BatchNorm + Dropout(0.1)` | 1,221,840 | 45.93% | 77.69% |
| 两卷积块 + 轻量仿射数据增强 | 1,221,840 | 44.36% | 79.13% |
| 两卷积块 + `ReduceLROnPlateau` | 1,221,840 | 46.19% | 77.43% |
| 三卷积块，固定学习率 | 1,240,464 | 57.74% | 84.78% |
| 三卷积块 + `ReduceLROnPlateau` | 1,240,464 | 57.48% | 84.25% |
| 三卷积块 + 轻量仿射数据增强 | 1,240,464 | 67.72% | 89.24% |
| 四卷积块 + 轻量仿射数据增强 | 1,314,576 | 66.93% | 88.71% |
| 三卷积块 + `1×1` 瓶颈 `64→32` + 数据增强 | 634,352 | 69.03% | 89.90% |
| 三卷积块 + `1×1` 瓶颈 `64→16` + 数据增强 | 329,152 | 73.23% | 91.14% |
| 三卷积块 + `1×1` 瓶颈 `64→8` + 数据增强 | 176,552 | 72.97% | 91.08% |
| 四卷积块 + `1×1` 瓶颈 `128→16` + 数据增强 | 252,224 | 80.05% | 93.96% |
| 四卷积块 + `1×1` 瓶颈 `128→8` + 数据增强 | 175,144 | 80.58% | 94.42% |
| 四卷积块 + `128→8` + 数据增强 + AdamW `1e-3` | 175,144 | 82.15% | 94.62% |
| 上述配置 + Label smoothing `0.05` | 175,144 | **82.94%** | **94.82%** |
| AdamW `1e-3` + 仿射增强 + ColorJitter | 175,144 | 80.31% | 94.09% |
| 四卷积块 + `128→32` + 位置感知共享头 | **103,300** | **93.70%** | **98.29%** |

### 四卷积块多随机种子复验

固定四卷积块、轻量仿射数据增强、固定学习率 `0.001` 和 30 轮训练，分别使用 seed
`0`、`1`、`2` 比较 8/16 通道瓶颈：

| 瓶颈通道 | Seed 0 | Seed 1 | Seed 2 | 平均整图准确率 | 样本标准差 | 参数量 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 16 | 79.53% | 80.05% | 77.69% | 79.09% | 1.24 | 252,224 |
| **8** | **80.58%** | 78.48% | **79.00%** | **79.35%** | **1.09** | **175,144** |

8 通道的平均整图准确率高 0.26 个百分点，波动略小，同时参数量比 16 通道少 30.6%。
精度差异很小，但 8 通道在平均准确率、稳定性和模型规模上均不劣，因此选为推荐配置。

### AdamW 权重衰减实验

固定四卷积块、8 通道瓶颈、轻量仿射数据增强、学习率 `0.001` 和 30 轮训练，只改变
优化器及权重衰减系数。`weight_decay=0` 使用历史 Adam 基线，大于 0 时使用 AdamW：

| 权重衰减 | Seed 0 | Seed 1 | Seed 2 | 平均整图准确率 | 样本标准差 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 0（Adam） | 80.58% | 78.48% | 79.00% | 79.35% | **1.09** |
| `1e-4` | **81.89%** | 77.69% | 79.53% | 79.70% | 2.11 |
| `3e-4` | 81.10% | 77.95% | 79.00% | 79.35% | 1.60 |
| **`1e-3`** | 81.63% | **78.48%** | **82.15%** | **80.75%** | 1.99 |

`1e-4` 只将平均准确率提高 0.35 个百分点且波动增大，`3e-4` 与 Adam 基线平均值相同；
`1e-3` 在三个 seed 上分别提升 1.05、0 和 3.15 个百分点，平均提高 1.40 个百分点，约等于
每轮多正确识别 5.3 张验证图片，因此选为当前推荐权重衰减。它的随机种子波动仍高于 Adam
基线，后续实验仍应比较多 seed 平均值，不能只使用 82.15% 的单次最高值。

在 `1e-3` 三次实验的最佳验证轮次，训练整图准确率分别为 98.62%、97.50% 和 98.12%，
与验证准确率仍相差约 16～19 个百分点，说明 AdamW 带来了小幅泛化收益，但没有消除过拟合。

### Label smoothing 实验

固定 AdamW 权重衰减 `1e-3` 及其余推荐条件，只将交叉熵的标签平滑系数从 0 调整为
`0.05`：

| 标签平滑 | Seed 0 | Seed 1 | Seed 2 | 平均整图准确率 | 样本标准差 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 0 | 81.63% | 78.48% | **82.15%** | 80.75% | 1.99 |
| `0.05` | **82.94%** | **80.58%** | 79.79% | **81.10%** | **1.64** |

标签平滑在 Seed 0、1 上分别提升 1.31 和 2.10 个百分点，但 Seed 2 下降 2.36 个百分点；
三 seed 平均只提高 0.35 个百分点，约等于多正确 1.3 张验证图片。样本标准差从 1.99 降至
1.64，但平均提升低于预先设定的约 1 个百分点保留门槛，因此目前不把 `0.05` 加入推荐配置。
标签平滑损失包含固定的平滑惩罚项，新旧实验的 loss 绝对值不能直接比较，应以准确率为主。

三个最佳验证轮次的训练整图准确率仍为 98.88%、98.38% 和 98.38%，平均训练—验证差距
约 17.44 个百分点，说明 `0.05` 没有明显缓解过拟合。

### ColorJitter 实验

固定 AdamW 权重衰减 `1e-3` 及其余推荐条件，在轻量仿射增强后增加 ColorJitter：

| 颜色增强 | Seed 0 | Seed 1 | Seed 2 | 平均整图准确率 | 样本标准差 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 关闭 | **81.63%** | **78.48%** | **82.15%** | **80.75%** | 1.99 |
| ColorJitter | 80.31% | 78.22% | 79.53% | 79.35% | **1.06** |

ColorJitter 在三个 seed 上分别下降 1.32、0.26 和 2.62 个百分点，平均下降 1.40 个
百分点。虽然样本标准差下降，但模型只是更稳定地收敛到较低准确率，因此不加入推荐配置。
三个最佳验证轮次的训练整图准确率分别为 95.62%、96.25% 和 97.50%，平均训练—验证差距
约 17.10 个百分点，与关闭颜色增强时约 17.33 个百分点基本相同，也没有明显缓解过拟合。

### 位置感知共享分类头实验

固定四卷积块、AdamW 权重衰减 `1e-3`、轻量仿射增强和其他训练条件，将 8 通道展平
分类头替换为 32 通道位置感知共享分类头：

| 分类头 | 参数量 | Seed 0 | Seed 1 | Seed 2 | 平均整图准确率 | 样本标准差 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Flatten + 瓶颈 8 | 175,144 | 81.63% | 78.48% | 82.15% | 80.75% | 1.99 |
| **Position + 瓶颈 32** | **103,300** | **93.70%** | **91.08%** | **92.91%** | **92.56%** | **1.34** |

位置感知分类头在三个 seed 上分别提升 12.07、12.60 和 10.76 个百分点，平均提升 11.81 个
百分点，相当于平均多正确识别约 45 张验证图片。模型参数量同时减少 41.0%，随机种子样本
标准差从 1.99 降到 1.34，因此它在精度、规模和稳定性三方面均明显优于展平分类头。

训练阶段显示的准确率来自开启 Dropout 且经过随机仿射增强的当前训练批次，而验证准确率来自
`model.eval()` 下的未增强图片，两者不是同条件测量。位置分类头出现验证准确率高于训练准确率
是合理现象，不能据此判断数据泄漏或欠拟合。现在可以通过 `--evaluate-clean-train` 每轮增加
关闭增强和 Dropout 的干净训练集评估指标，再与验证集进行同条件比较。

### 位置分类头瓶颈通道实验

保持位置分类头、Dropout `0.1`、AdamW 权重衰减 `1e-3` 和轻量仿射增强不变，只比较
16、32、64 个瓶颈通道。表中的干净训练准确率取各次最高验证准确率所在轮次：

| 瓶颈通道 | Seed 0 | Seed 1 | Seed 2 | 验证均值 | 样本标准差 | 干净训练均值 | 平均泛化差距 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 16 | 88.98% | 92.91% | 87.93% | 89.94% | 2.63 | 93.08% | 3.14 |
| 32 | 95.28% | 93.44% | 日志缺失 | 94.36%（2 seeds） | 1.30（2 seeds） | 96.50% | 2.14 |
| **64** | **95.28%** | **94.23%** | **93.96%** | **94.49%** | **0.70** | **97.87%** | 3.38 |

16 通道的表达能力明显不足。64 通道三次实验均超过 93.9%，平均达到 94.49%，而且波动最小；
32 通道现有两次实验平均为 94.36%，与 64 通道只差 0.13 个百分点，但缺少 Seed 2，暂时无法
进行完整的三 seed 公平比较。因此当前把 64 通道作为**暂定推荐配置**，主要依据是它具有完整且
稳定的三 seed 结果，而不是 0.13 个百分点这一不足以证明优势的均值差异。

64 通道最佳验证轮次的干净训练—验证差距分别为 2.72、3.27 和 4.16 个百分点，平均 3.38 个
百分点。与早期展平分类头约 17 个百分点的差距相比，过拟合已大幅缓解，但仍有轻度泛化差距。

### 64 通道 Dropout 实验

固定 64 通道位置分类头及其他训练条件，只改变分类头 Dropout。表中的干净训练准确率取各次
最高验证准确率所在轮次：

| Dropout | Seed 0 | Seed 1 | Seed 2 | 验证均值 | 样本标准差 | 干净训练均值 | 平均泛化差距 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| **0** | **96.85%** | 93.70% | **94.75%** | **95.10%** | 1.60 | **98.33%** | **3.23** |
| `0.05` | 93.96% | **96.33%** | 93.96% | 94.75% | 1.37 | 98.29% | 3.54 |
| `0.1` | 95.28% | 94.23% | 93.96% | 94.49% | **0.70** | 97.87% | 3.38 |
| `0.2` | 95.80% | 91.60% | 92.13% | 93.18% | 2.29 | 96.50% | 3.32 |

`0.05` 的平均准确率为 94.75%，介于关闭 Dropout 和 `0.1` 之间；样本标准差 1.37，只比关闭
Dropout 的 1.60 小 0.23，未同时取得更高均值或接近 `0.1` 的稳定性。`0.2` 平均下降到
93.18%，说明增强 Dropout 会造成正则化过强。四组平均泛化差距都约为 3.2～3.5 个百分点，
Dropout 并没有实质缩小剩余差距。

如果优先考虑三 seed 平均准确率，关闭 Dropout 的 95.10% 是当前最佳；如果优先考虑不同 seed
下的稳定性，`0.1` 的标准差 0.70 更好。本项目当前目标是在固定样本量下逐步提高平均准确率，
因此将推荐值调整为 `0`，同时明确它相对 `0.1` 的平均提升只有 0.61 个百分点，证据强度有限。

### 训练轮数实验

关闭 Dropout 的三次 30 轮实验最佳结果都出现在第 28～30 轮，因此只将训练轮数增加到 40：

| 训练轮数 | Seed 0 | Seed 1 | Seed 2 | 验证均值 | 样本标准差 | 干净训练均值 | 平均泛化差距 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 30 | **96.85%** | 93.70% | 94.75% | 95.10% | 1.60 | **98.33%** | 3.23 |
| **40** | **96.85%** | **95.54%** | **95.01%** | **95.80%** | **0.95** | 98.29% | **2.49** |

40 轮在 Seed 0 上没有超过第 30 轮的结果，但 Seed 1 和 Seed 2 分别提升 1.84 和 0.26 个
百分点；三 seed 平均提高 0.70 个百分点，样本标准差从 1.60 降到 0.95，平均泛化差距也从
3.23 降到 2.49 个百分点。虽然平均提升不足 1 个百分点，但精度、稳定性和泛化差距均改善，
因此将推荐训练轮数更新为 40。

### 当前位置分类头学习率调度实验

保持 40 轮和其他条件不变，只启用 `ReduceLROnPlateau`：

| 学习率策略 | Seed 0 | Seed 1 | Seed 2 | 验证均值 | 样本标准差 | 干净训练均值 | 平均泛化差距 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 固定 `0.001` | **96.85%** | 95.54% | 95.01% | 95.80% | 0.95 | 98.29% | 2.49 |
| **ReduceLROnPlateau** | **96.85%** | **97.38%** | **97.38%** | **97.20%** | **0.31** | **99.46%** | **2.26** |

调度器分别在 Seed 0、1、2 的第 38、32、33 轮后把学习率从 `0.001` 降到 `0.0003`。
Seed 0 的历史最佳仍是第 30 轮的 96.85%，因此结果不变；Seed 1 和 Seed 2 分别提高 1.84 和
2.37 个百分点。三 seed 平均提高 1.40 个百分点，样本标准差从 0.95 降到 0.31，平均泛化
差距也从 2.49 降到 2.26 个百分点，因此在当前位置分类头上明确保留该调度器。

### 调度器下的训练轮数复验

保持调度器及其他条件不变，把训练轮数从 40 增加到 50：

| 训练轮数 | Seed 0 | Seed 1 | Seed 2 | 验证均值 | 样本标准差 | 干净训练均值 | 平均泛化差距 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| **40** | 96.85% | 97.38% | **97.38%** | 97.20% | 0.31 | 99.46% | **2.26** |
| 50 | **97.11%** | **97.64%** | **97.38%** | **97.38%** | **0.27** | 100.00% | 2.62 |

50 轮在 Seed 0 和 Seed 1 上各提高 0.26 个百分点，Seed 2 不变；三 seed 平均只提高 0.18 个
百分点，约等于平均多正确 0.7 张验证图片，未达到保留门槛。与此同时，最佳轮次的干净训练集
已经全部达到 100%，平均泛化差距反而从 2.26 增到 2.62 个百分点，且训练计算量增加 25%。
因此不保留 50 轮，当前推荐仍为 40 轮并启用调度器。

50 轮最佳验证轮次分别为第 47、50、40 轮。虽然个别 seed 在后十轮仍有小幅提升，但整体收益
已经接近验证集单张图片造成的离散波动，因此后续没有继续增加轮数或盲目微调 Dropout，而是转向
独立评估和合成数据鲁棒性实验。最终测试现已锁定，不再根据其中的错误样本继续调整模型。

早期三卷积块实验中该调度器没有收益，但当前位置分类头的结果已经证明，旧结构上的结论不能
直接套用于新结构。

实验结论：

- 原模型约 99.9% 的参数集中在全连接层，增加一次温和的 `2×2` 平均池化后，模型参数量
  从约 519 万降至约 122 万，减少约 76.5%，同时整图准确率有所提升。
- 将特征图直接压缩至 `5×15` 会丢失过多字符空间信息，整图准确率明显下降，因此不采用。
- `Dropout(0.3)` 对当前小型 CNN 正则化过强；`Dropout(0.1)` 能提供小幅提升。
- 两个卷积层采用 `Conv2d → BatchNorm2d → ReLU → MaxPool2d` 后，收敛速度和验证指标
  均有明显改善；其中 BatchNorm 与 Dropout(0.1) 同时使用时达到 45.93% 整图准确率。
- 增加第三个 `32 → 64` 卷积块后，固定学习率实验达到 57.74%，比两卷积块基线提升
  11.81 个百分点，对应 381 张验证图片中多识别正确 45 张。第三个卷积块显著增强了对字符
  笔画、轮廓和干扰线组合特征的提取能力。
- 早期三卷积块结构使用 `ReduceLROnPlateau` 时达到 57.48%，比固定学习率少正确 1 张，
  因而当时未采用；该结论只适用于旧结构。
- 三卷积块配合轻量仿射数据增强后，第 27 轮同时取得最低验证 loss `0.4360`、最高整图
  准确率 67.72% 和最高单字符准确率 89.24%。相比不增强的三卷积块多识别正确 38 张，
  相比两卷积块基线总计提升 21.79 个百分点。
- 四卷积块不使用瓶颈时达到 66.93% 整图准确率和 88.71% 单字符准确率，增加网络深度
  但保留大型全连接层没有带来提升。
- 三卷积块的 `64→32`、`64→16` 和 `64→8` 通道瓶颈分别达到 69.03%、73.23% 和
  72.97% 整图准确率，证明压缩分类头能够改善泛化，但三卷积块在 8 通道时已接近边界。
- 四卷积块多 seed 复验中，16 通道和 8 通道的平均整图准确率分别为 79.09% 和 79.35%；
  8 通道样本标准差更低，并以 175,144 个参数比 16 通道减少约 30.6%，因此综合胜出。
- AdamW 权重衰减实验中，`1e-3` 将三 seed 平均整图准确率从 79.35% 提高到 80.75%，
  单次最高达到 82.15%；`1e-4` 和 `3e-4` 的平均提升不足，因此不采用。
- Label smoothing `0.05` 的三 seed 平均准确率为 81.10%，只比无标签平滑提高 0.35 个
  百分点，未达到保留门槛；保留命令行能力用于复验，但当前推荐值仍为 0。
- ColorJitter 将三 seed 平均准确率从 80.75% 降到 79.35%，且几乎没有缩小训练—验证差距，
  因此只保留命令行能力，不加入推荐配置。
- 32 通道位置感知共享分类头首次将三 seed 平均准确率从 80.75% 提高到 92.56%，参数量从
  175,144 降到 103,300，是当前最明显的一次结构提升。
- 后续通道实验中，16 通道平均准确率只有 89.94%；64 通道完整三 seed 平均达到 94.49%，
  样本标准差为 0.70。32 通道目前只有两个可用 seed，平均为 94.36%，因此暂不能证明 64
  通道在平均准确率上显著胜出，但 64 通道具有更完整、稳定的现有证据。
- 64 通道 Dropout 实验中，关闭 Dropout 的平均准确率最高，为 95.10%；`0.05`、`0.1` 和
  `0.2` 分别为 94.75%、94.49% 和 93.18%。Dropout 没有缩小约 3.3 个百分点的泛化差距，
  且强度越高平均准确率总体越低，因此按当前精度优先目标关闭 Dropout。
- 将训练从 30 轮增加到 40 轮后，平均准确率从 95.10% 提高到 95.80%，标准差从 1.60 降到
  0.95，平均泛化差距从 3.23 降到 2.49 个百分点，因此保留 40 轮训练。
- 在 40 轮新基线上启用 `ReduceLROnPlateau` 后，平均准确率进一步提高到 97.20%，标准差降到
  0.31，平均泛化差距降到 2.26 个百分点，因此新结构明确保留学习率调度。
- 调度器下增加到 50 轮只将平均准确率提高到 97.38%，增幅 0.18 个百分点，同时干净训练集
  达到 100%、泛化差距增至 2.62 个百分点且计算量增加 25%，因此不保留 50 轮。
- 75% 合成训练比例在 Seed 0、1、2 上的原始验证整图准确率分别为 96.59%、96.59% 和
  96.33%，平均 96.50%、样本标准差 0.15；对应合成验证平均整图准确率为 96.25%、样本标准差
  0.90。相比纯真实训练的 97.20% 原始验证平均值下降 0.70 个百分点，但显著改善合成鲁棒性。
- 当前推荐配置为四卷积块、`128→64` 通道瓶颈、位置感知共享分类头、关闭 Dropout、40 轮、
  初始学习率 `0.001`、`ReduceLROnPlateau`、AdamW 权重衰减 `1e-3`、轻量仿射数据增强和
  75% 合成训练比例；不启用 ColorJitter 或 Label smoothing。当前 V1 冻结权重使用 Seed 2。
- 本项目以学习和实验记录为主，训练默认覆盖 `stage2_simple_cnn/checkpoints/model.pth`，
  不按卷积块、瓶颈通道或 seed 长期保留独立权重，也不提交训练生成的 `.pth` 文件。

上述指标来自反复参与模型选择的 `data/test`，因此属于验证结果，不是独立测试集上的最终
识别率。

## 第二阶段结项总结

### 结项判断

第二阶段以“从零构建一个可解释、可复验并具备基本跨渲染泛化能力的轻量 CNN”为学习目标，现已
完成。项目不再围绕当前验证集和最终测试集继续追求零点几个百分点的微调；后续工作如继续开展，
应转向真实业务数据、推理工程、模型导出或置信度校准，并建立新的独立测试集评价新版本。

### 最终方案

| 项目 | 结项配置 |
| --- | --- |
| 网络 | 四卷积块 Simple CNN |
| 瓶颈 | `128→64` 的 `1×1` 卷积 |
| 分类头 | 位置感知、四位置共享的 `64→36` 分类器 |
| 参数量 | 108,644 |
| 输入/输出 | `[B, 3, 100, 180]` → `[B, 4, 36]` |
| 优化器 | AdamW，weight decay `0.001` |
| 训练策略 | 40 轮、初始学习率 `0.001`、`ReduceLROnPlateau` |
| 正则与增强 | Dropout 0、轻量仿射增强、不使用 ColorJitter 或 Label smoothing |
| 数据组成 | batch size 16，每批 4 张真实图片和 12 张合成图片 |
| V1 冻结 Seed | Seed 2；Seed 0、1、2 只用于验证配置稳定性 |

### 结项指标

| 数据集 | 整图准确率 | 单字符准确率 | 说明 |
| --- | ---: | ---: | --- |
| 原始验证集，75% 合成三 Seed 平均 | 96.50% | 99.06% | 配置稳定性；样本标准差分别为 0.15、0.03 |
| 合成验证集，75% 合成三 Seed 平均 | 96.25% | 99.02% | 配置稳定性；样本标准差分别为 0.90、0.24 |
| 原始验证集，冻结 Seed 2 权重 | 96.33% | 99.02% | 与最终测试使用同一权重 |
| 合成验证集，冻结 Seed 2 权重 | 96.50% | 99.12% | 与最终测试使用同一权重 |
| 最终独立合成测试集，冻结 Seed 2 权重 | **95.75%** | **98.94%** | 383/400 整图正确，1583/1600 字符正确 |

“整图准确率”要求一张图片的 4 个位置全部预测正确；“单字符准确率”按全部图片的正确位置数计算。
以上测试只覆盖实际数据中的 20 个字符，而不是分类层保留的完整 36 类。

最终测试使用 seed `20250310` 的 400 张冻结图片。除评估器默认检查 `data/train` 和 `data/test`
外，生成后还单独审计了 `data/synthetic_train` 和 `data/synthetic_validation`，与四个已知目录的
SHA-256 重复数均为 0。审计结果保存在本地 `data/independent_test/final_audit.json`。

该结果属于当前 V1 模型，不再用于后续调参。由于最终集仍来自同一生成器家族，它证明了新图片与
新渲染组合下的稳定性，但不能替代真实业务来源的新采集测试数据。自动字体发现还依赖本机字体池；
跨机器严格复现时必须显式提供相同的 `--font-path`。

### 主要学习结论

1. 原始模型的大型全连接层是主要参数和过拟合来源；位置感知共享分类头把参数量压缩到 108,644，
   同时显著提升验证准确率。
2. 模型深度、瓶颈通道、正则化和训练轮数需要通过控制变量与多 Seed 实验判断，单次最高值不足以
   支撑结构选择。
3. `ReduceLROnPlateau` 对早期结构没有收益，但在最终位置感知结构上明显改善后期收敛，说明训练
   策略结论不能脱离具体模型结构复用。
4. 纯真实训练在原始验证集表现较好，却在独立渲染上存在严重域偏移；固定批次比例混合合成数据后，
   原始与合成验证准确率均稳定在约 96%。
5. 75% 合成比例的收益包含更多合成样本和更多优化步骤，并非严格控制计算量的纯比例消融结果。
6. 验证集、合成鲁棒性验证集和最终测试集必须分工明确；测试集一旦影响训练决策，就不再是最终
   测试集。

### 阶段产物与状态

- 训练入口：`stage2_simple_cnn/train.py`
- 固定比例采样：`stage2_simple_cnn/data_loading.py`
- 合成生成器：`stage2_simple_cnn/generate_captchas.py`
- 独立评估与混淆矩阵：`stage2_simple_cnn/evaluate.py`
- 当前共享权重：`stage2_simple_cnn/checkpoints/model.pth`，Seed 2、75% 合成比例
- 最终测试报告：`stage2_simple_cnn/evaluation_reports/`
- 最终测试数据：`data/independent_test/`，仅本地冻结，不提交 Git
- 冻结身份与重复审计：`data/independent_test/final_audit.json`

**阶段状态：第二阶段完成，V1 模型与最终测试结果冻结，停止准确率微调。**

## 第三阶段：CNN + CTC 可变长度识别

第三阶段以第二阶段四卷积块 CNN 为特征提取基础，从随机初始化开始学习 2～6 位可变长度字符识别。
第一版保持输入画布为 `100×180`，使用字符表 `2345678abcdefgmnpwxy`，暂不加入 LSTM。
模型保留 CNN 输出的 22 列横向特征作为时间步，经高度平均和共享线性分类器后输出
`[B, 22, 21]`；21 类包含 20 个字符和一个 CTC blank。

### 极小数据集过拟合诊断

首次链路诊断使用 10 张内存生成的固定干净图片，覆盖 2～6 位长度，并包含 `aa`、`33`、`xx`
等连续重复字符。训练使用 batch size 10、Adam、学习率 `0.003`、seed 0 和 MPS，不保存诊断
checkpoint。模型从随机初始化训练，在第 109 轮达到 100% 训练集整串准确率，loss 从第 1 轮的
15.3197 降至 0.0804；10 条标签均被贪心 CTC 解码正确。

该结果只证明图片加载、CNN 序列输出、可变长度标签编码、CTC loss、反向传播和贪心解码已经形成
可工作的训练闭环，不代表模型对未见图片具有泛化能力。

### 正式合成数据 V1

正式数据使用 seed `20250311` 和中等难度动态布局生成器，输入画布为 `100×180`。训练集、验证集和
测试集分别包含 1,500、250 和 250 张图片；2～6 位每种长度分别占 300、50 和 50 张。三个集合从
全局唯一标签中按长度分层切分，因此标签交集为 0；生成后的 SHA-256 审计也确认图片内容交集为 0。
本地数据及每个集合的 `manifest.json` 位于 `data/stage3_variable_length/`，不提交 Git。正式 Dataset
以 manifest 作为标签和图片身份来源，而不依赖文件名解析。

### CNN + CTC 正式基线

正式基线使用当前 107,669 参数的四卷积块 CNN、64 通道瓶颈和贪心 CTC 解码，从随机初始化训练。
配置为 Adam、学习率 `0.001`、batch size 16、40 轮、seed 0，不使用额外在线增强或学习率调度器。
训练过程只使用训练集和验证集，没有读取冻结测试集。

最佳共享 checkpoint 出现在第 31 轮，验证整串准确率为 86.00%，CER 为 4.30%，验证 loss 为
0.1565。按目标长度的整串准确率分别为：2 位 86.00%、3 位 94.00%、4 位 90.00%、5 位
90.00%、6 位 70.00%。该共享文件由相同 seed 和训练参数、启用 `evaluate_train=True` 的复验覆盖；最佳轮次和验证指标与首次训练一致。

训练 loss 从 7.0046 持续降至 0.0015，而验证整串准确率在约第 17 轮后主要波动于 84%～86%。
随后使用第 31 轮最佳 checkpoint 在 `model.eval()` 下同条件复评：训练集 loss 为 0.0023，整串准确率
为 100.00%，CER 为 0.00%，2～6 位各长度准确率均为 100.00%；对应验证集整串准确率为
86.00%、CER 为 4.30%。因此整串准确率泛化差距为 14.00 个百分点，确认当前基线已完全记住训练集，
但对未见标签和渲染仍有明显泛化差距。训练入口可通过 `--evaluate-train` 在每轮参数更新后用
`model.eval()` 重新遍历训练集，输出训练整串准确率、CER、按长度指标和训练—验证泛化差距；该选项
会增加一次训练集前向遍历，但不会影响最佳模型仍只按验证指标选择。在方案确定前继续冻结测试集。

### 正式基线验证错误分析

第 31 轮最佳 checkpoint 在 250 张验证图片上产生 35 条错误字符串和 43 次最短编辑操作，其中
27 条错误只差 1 次编辑，另外 8 条差 2 次编辑。字符级操作包括 24 次替换、16 次删除和 3 次插入；
按整条字符串的主要类型统计为 17 条纯替换、10 条纯删除、3 条纯插入和 5 条混合错误。因此当前问题以
视觉替换和漏字符为主，额外插入不是主要矛盾。

六位样本贡献 15/35 的错误字符串、21/43 的编辑距离和 11/16 的删除操作，说明六位布局中的小字符、
拥挤或干扰更容易造成漏识别。连续重复字符样本为 34/39 正确（87.18%），略高于非重复样本的
181/211（85.78%），因此 CTC 连续重复折叠不是整体瓶颈。替换错误集中在 `m↔n`（7 次）、`8↔g`
（4 次）、`d↔a`（3 次）和 `n→w`（2 次）；这些组合占 24 次替换中的 16 次。代表图片还显示交叉
干扰线会遮挡 `8`，六位低对比度小字符容易混淆，而 `4fwfaf` 的首部字符和干扰线交叠时发生连续删除。
下一次控制变量实验应优先提高训练渲染多样性并针对六位布局，而不是继续增加训练轮数；是否加入序列模型
应在数据侧实验后再决定。

### 训练渲染多样性 V2 实验设计

V2 保持原 1,500 个训练标签不变，并为每个标签保留 V1 原图、增加 1 张 seed `20250312` 的独立新渲染，
形成 3,000 张训练图片和 1,500 个唯一标签。验证集和测试集保持 V1 不变；生成器只读取测试 manifest 的
标签与 SHA-256 做身份泄漏审计，不运行模型或计算测试指标。审计还检查 V2 训练内部图片 SHA 是否重复。

为了近似保持总样本呈现次数和优化器更新次数不变，基线使用 `1,500 × 40 = 60,000` 次样本呈现，V2
计划使用 `3,000 × 20 = 60,000` 次样本呈现；其余模型结构、Adam、学习率 `0.001`、batch size 16、
seed 0、验证集和贪心解码保持一致。该实验主要检验增加同标签视觉版本能否降低 `m/n`、`8/g` 等替换和
六位漏字符，而不是检验更长训练时间。正式 V2 数据已按计划生成：2～6 位每种长度各 600 张，训练与
验证/测试标签交集、图片 SHA-256 交集以及训练内部重复 SHA-256 均为 0。

### 训练渲染多样性 V2 首次结果

V2 等预算训练使用 3,000 张图片、20 轮、Adam、学习率 `0.001`、batch size 16 和 seed 0。最佳共享
checkpoint 出现在第 12 轮：训练整串准确率 92.63%、训练 CER 2.02%，验证整串准确率 76.80%、
验证 CER 7.40%；各长度验证准确率为 78%、78%、78%、80% 和 70%。相比 V1 的 86.00% 和
4.30%，V2 首次结果明显退化。

该训练还出现显著评价震荡，例如训练整串准确率从第 16 轮的 97.40% 跌至第 17 轮的 66.33%，第 18 轮
回升到 89.13%，第 20 轮又降至 46.00%；因此不能直接把退化解释为“更多渲染无效”。只读诊断显示最佳
checkpoint 在 V1 原图子集和新增渲染子集上的准确率分别为 93.20% 和 92.07%，差异仅 1.13 个百分点，
不支持新增渲染分布严重失配。重新校准 BatchNorm 后训练/验证准确率从 92.63%/76.80% 提高到
95.67%/79.60%，说明运行统计有部分影响，但仍未恢复 V1 水平。下一步应检查逐 batch loss 与梯度范数，
判断学习率 `0.001` 是否对更复杂的 V2 优化造成参数跳变；在完成该诊断前不进行测试集评价。

训练入口现支持 `--diagnose-batches`：在不改变反向传播和 Adam 更新结果的前提下，逐轮汇总最大 batch
loss、对应 batch 编号、最后 batch loss、最大梯度 L2 范数、对应编号、最后梯度范数以及非有限值计数。
诊断复现运行到首次震荡后的第 12 轮，并保持 V2 数据、学习率和 seed 不变。所有 epoch 的 loss 和梯度
非有限计数均为 0。第 10 轮评价崩落时最大梯度范数为 6.4083，仅比第 9 轮的 5.6284 高约 14%，最后
batch 的 loss/梯度范数仅为 0.1096/1.3819；第 12 轮最后 batch 反而同时产生本轮最大 loss 0.3276 和
最大梯度范数 5.4052，但评价恢复到 76.80%。因此没有证据支持梯度爆炸或最后 batch 单独破坏模型。
当前最强假设转为 BatchNorm 运行统计跟不上快速变化的激活分布，需要在同一 epoch 对普通 eval、当前
batch 统计和重新校准统计做差分评价。

训练入口进一步支持 `--diagnose-batchnorm`。每轮先保留正常 `model.eval()` 指标，再复制当前模型，只将副本
中的 `BatchNorm2d` 切换为当前 batch 统计，分别评价训练集和验证集，并输出准确率和 CER 差值。副本中的
运行均值/方差变化会被丢弃，不修改原模型，也不参与最佳 checkpoint 选择。该差分将直接检验第 10～11 轮
崩落是否只发生在冻结的 BatchNorm 运行统计路径。

差分结果确认第 10～11 轮的灾难性下降主要来自运行统计：第 10 轮普通/batch-stat验证准确率为
49.20%/71.60%，相差 22.40 个百分点，CER 从 20.40% 降至 8.60%；第 11 轮为 49.20%/72.00%，
相差 22.80 个百分点，CER 从 18.00% 降至 8.10%。第 12 轮差距收缩到 2.00 个百分点，说明运行统计
随后重新追上当前权重。batch-stat 指标使用验证 batch 自身统计，只是诊断值，不能用于模型选择；下一步应
使用训练数据重新校准模型副本的 BatchNorm 统计，再以标准 `model.eval()` 评价和保存可部署 checkpoint。

训练入口现支持 `--recalibrate-batchnorm`。启用后每轮复制训练模型，重置副本全部 BatchNorm 运行统计，
只用训练集无梯度前向传播和累计平均重新构建统计，再在标准 `model.eval()` 下评价训练集与验证集。原训练
模型不被修改，并继续下一轮；最佳模型改为按校准后验证指标选择，保存的也是带训练集校准统计的副本。
日志同时保留校准前验证指标和校准后的准确率/CER差值。该模式与使用验证 batch 自身统计的
`--diagnose-batchnorm` 互斥，确保正式checkpoint不吸收验证集统计。

V2 完整 20 轮校准实验消除了虚假崩落：例如第 20 轮校准前/后验证准确率为 34.00%/74.00%，说明
40 个百分点的下降来自过时运行统计。校准后最佳模型出现在第 16 轮，训练整串准确率 99.30%、CER
0.18%，验证整串准确率 80.40%、CER 5.90%，按长度为 80%、82%、78%、88% 和 74%；泛化差距为
18.90 个百分点。相比未校准 V2 提高 3.60 个百分点并降低 1.50 个百分点 CER，但仍比 V1 低 5.60 个
百分点、CER 高 1.60 个百分点。因此 BatchNorm 校准修复了评价与checkpoint统计问题，但双渲染 V2 在
相同优化步预算下仍未超过 V1。

V2 校准checkpoint的验证错误分析包含 49 个错误字符串和 59 次编辑，其中替换/删除/插入为
43/14/2；相比 V1 的 35 个错误字符串、43 次编辑和 24/16/3，新增的 16 次编辑几乎全部来自替换增加
19 次，而删除减少 2 次、插入减少 1 次。六位准确率从 70% 提高到 74%，但编辑距离仅从 21 降至 20；
2～4 位准确率分别下降 6、12、12 个百分点。`8↔g` 从 4 次降至 2 次、`d↔a` 从 3 次降至 1 次，
但 `m↔n` 从 7 次增至 12 次。连续重复字符准确率从 87.18% 降到 71.79%（错误从 5 个增至 11 个）。
因此固定双渲染只略微改善长序列删除和部分视觉混淆，却引入大量新的替换错误；在训练准确率 99.30% 的
情况下，这更像泛化/正则化问题，而不是继续增加恒定学习率训练轮数即可解决的欠拟合。

训练入口现支持非负 `--weight-decay`，默认值 0 保持历史实验不变，并直接传入原有 Adam 优化器。首个
正则化对照只把该值设为 `0.0001`，其余保持 V2、20 轮、batch size 16、学习率 0.001、seed 0 和每轮
BatchNorm 训练集重校准不变。目标是检验轻量 L2 正则化能否缩小 99.30%/80.40% 的训练—验证差距；结果
仍按校准后的标准验证准确率、CER、loss 依次选择，测试集继续冻结。

`weight_decay=0.0001` 的最佳模型出现在第 14 轮：训练准确率 96.10%、CER 1.00%，验证准确率
79.20%、CER 5.90%，按长度为 84%、84%、74%、80% 和 74%，泛化差距 16.90 个百分点。相比无权重
衰减的校准 V2，训练准确率下降 3.20 个百分点，验证准确率也下降 1.20 个百分点，CER 持平；差距缩小是
训练拟合受损而非验证改善，因此该正则化强度不成功。2～3 位分别提高 4、2 个百分点，但 4～5 位分别下降
4、8 个百分点，六位持平。

第三阶段现复用第二阶段的 `ReduceLROnPlateau` 思路，通过 `--reduce-lr-on-plateau` 可选启用。适配后的
固定设置为 `mode=min`、`factor=0.3`、`patience=1`、`min_lr=1e-5`；监控值是训练集重新校准
BatchNorm 后的标准验证 loss，避免校准前运行统计震荡错误触发调度器。每轮日志显示实际学习率，并在下降
时打印调整前后值；checkpoint记录调度器开关。首个对照恢复 `weight_decay=0`，其余保持 V2、20 轮、
初始学习率 0.001、batch size 16、seed 0 和每轮 BatchNorm 校准不变。

自动调度实验在第 18 轮后把学习率从 0.001 降至 0.0003，第 19 轮立即取得最佳结果：训练准确率
99.93%、CER 0.02%，验证准确率 82.00%、CER 5.80%，按长度为 84%、84%、82%、88% 和 72%。
相比固定学习率校准 V2，验证准确率提高 1.60 个百分点、CER 降低 0.10 个百分点；第 19 轮在相同位置
也比固定学习率高 4 个百分点，支持后期较小更新步幅更合适。

该 checkpoint 的验证错误分析包含 45 个错误字符串和 58 次编辑，替换/删除/插入为 41/17/0。相比
固定学习率 V2，错误字符串减少 4 个，但总编辑距离只减少 1：替换减少 2、插入减少 2，删除增加 3。
连续重复字符从 28/39 提高到 30/39，但仍低于 V1 的 34/39；`m↔n` 从固定 V2 的 12 次增至 13 次，
仍远高于 V1 的 7 次。自动调度主要改善 2～4 位整串正确数，没有解决视觉替换、重复字符和长序列删除。

### CNN + BiLSTM + CTC 实验

第三阶段模型现支持 `sequence_model=cnn|bilstm`，默认 `cnn` 保持历史checkpoint兼容。BiLSTM版本在
`[B,22,64]` CNN视觉序列后加入一层、每方向 hidden size 64 的双向 LSTM，拼接为 `[B,22,128]` 后
映射到 21 类CTC logits；输出形状仍为 `[B,22,21]`，参数量从 107,669 增至 175,573。训练CLI通过
`--sequence-model bilstm --lstm-hidden-size 64` 启用，并与V2数据、BatchNorm训练集重校准和自动学习率
兼容。checkpoint新增模型架构元数据；评价与验证错误分析会自动按元数据重建模型，同时旧checkpoint缺少
该字段时仍默认加载纯CNN。首个CRNN对照保持20轮、batch size 16、初始学习率0.001、weight decay 0、
seed 0、贪心解码和模型选择规则不变，只增加BiLSTM序列层。

首个BiLSTM实验在第15轮后由调度器把学习率从0.001降至0.0003，并在第18轮取得最佳结果：训练准确率
99.93%、CER 0.02%，验证准确率93.60%、CER 1.70%，按长度为96%、98%、100%、98%和76%，
泛化差距6.33个百分点。相比V2纯CNN自动调度模型，验证准确率提高11.60个百分点、CER降低4.10个
百分点，全部长度分别提高12、14、18、10和4个百分点；也全面超过V1的86.00%/4.30%。这确认显式双向
序列上下文是当前任务的关键缺失模块。

BiLSTM验证错误分析只剩16个错误字符串和17次编辑，其中替换/删除/插入为14/3/0；相比自动调度纯CNN
分别减少29个错误字符串、41次编辑、27次替换和14次删除。`m↔n` 从13次降到2次，连续重复字符从
30/39提高到33/39。剩余错误高度集中于六位：12/16个错误来自六位，六位12个错误的编辑距离都恰好为1，
全部3次删除也都发生在六位；2～5位合计只错4个字符串。下一步适合保持权重不变，对比CTC贪心解码与
无语言模型的prefix beam search，检验汇总多条CTC路径能否修复密集六位和重复字符错误。

评价与错误分析入口现支持 `--decoder greedy|beam --beam-width N`。prefix beam search在log-space中分别
累计前缀以blank和非blank结尾的概率，正确处理重复字符必须经blank扩展的CTC规则；第一版固定beam width
为10且不加入语言模型。BiLSTM checkpoint的beam-10结果与greedy完全一致：250个验证样本的全部预测、
16个错误明细、93.60%准确率、1.70% CER、六位76%和重复字符33/39均无变化。因此剩余错误不是贪心只取
单条路径造成的，当前模型在这些样本上已经让错误字符串成为汇总路径概率最高的候选；继续扩大beam而不增加
新信息缺乏依据。

### 提高CTC时间分辨率实验

第三阶段模型现支持 `--sequence-width {22,45}`，默认22兼容历史checkpoint。T=45模式把四块CNN中第三个
池化层从 `(2,2)` 改为 `(2,1)`，只继续压缩高度而保留宽度，使瓶颈输出由 `[B,64,3,22]` 变为
`[B,64,3,45]`，BiLSTM和CTC时间轴相应由22增至45；模型参数量保持175,573不变。训练配置、BatchNorm
校准副本和checkpoint架构元数据均记录该选项；旧checkpoint缺少字段时默认按22重建。首个T=45对照继续
使用V2数据、BiLSTM hidden size 64、20轮、batch size 16、初始学习率0.001、weight decay 0、seed 0、
BatchNorm训练集重校准、自动学习率和greedy解码，只改变时间分辨率。目标是改善当前集中于六位的12个近失
错误，尤其3次删除和连续重复字符。

首个T=45实验训练20轮后只达到训练45.13%/CER 21.93%、验证36.40%/CER 26.30%，按长度为78%、
48%、30%、24%和2%。但这不能解释为更高时间分辨率造成泛化退化：训练集本身尚未拟合，验证loss从
3.4063持续降到0.8120，第20轮仍在明显改善，调度器从未触发，学习率始终为0.001。T=45把每个样本的
CTC路径从22步扩展到45步，增加了blank位置和可行对齐数量，使从随机初始化发现单调对齐显著变慢；相同20轮
不再构成与T=22相同的优化进度。当前共享checkpoint为第20轮T=45模型。下一步应先解决如何从该checkpoint
延续训练并保留优化器/调度器状态，再判断T=45的最终泛化能力，而不是对欠拟合模型做错误类别分析。

随后从相同seed随机初始化完整训练40轮。T=45在第32轮后把学习率降至0.0003、第38轮后降至0.00009，
最终已充分拟合；最佳第39轮训练准确率99.50%、CER 0.12%，验证准确率87.60%、CER 3.90%，按长度为
96%、94%、92%、92%和64%，泛化差距11.90个百分点。相比T=22 BiLSTM的93.60%/1.70%，总体低6个
百分点、CER高2.20个百分点；2位相同，3/4/5/6位分别低4/8/6/12个百分点。尤其六位从76%降到64%，
否定了增加到45个时间步能改善密集长序列的假设。更多横向位置虽保留细粒度特征，却也减少池化带来的平移
稳定性、扩大CTC对齐空间并增加过拟合自由度；当前任务中T=22是更有效的压缩。共享checkpoint现为第39轮
T=45模型。

### 六位标签加权采样实验

训练入口现支持 `--length-6-weight`，默认1.0时保持原来的无放回shuffle；设为2.0时使用固定seed的
`WeightedRandomSampler`，每轮仍有3000次抽样且采用有放回采样，六位样本的单次权重是其他长度的2倍。
由于V2五种长度原本各600张，理论每轮分布由各600变为六位约1000、其他长度各约500；seed 0的一次抽样
得到2/3/4/5/6位分别485/491/545/471/1008，总数仍为3000。因此20轮仍是60000次样本呈现，没有用更多
总训练预算，只把约400次/轮更新从2～5位重新分配给六位。首个对照恢复T=22、BiLSTM hidden size 64，
其余继续使用V2、20轮、batch size 16、初始学习率0.001、weight decay 0、seed 0、BatchNorm重校准、
自动学习率和greedy解码。目标是在不明显损害2～5位的前提下把六位从76%提高，并观察总体93.60%是否保持。

2倍六位加权实验最佳第19轮：训练准确率99.83%、CER 0.04%，验证准确率92.40%、CER 2.10%，按长度为
98%、94%、96%、96%和78%，泛化差距7.43个百分点。相比未加权T=22基线，总体下降1.20个百分点、CER
增加0.40个百分点；2/3/4/5/6位分别变化+2/-4/-4/-2/+2个百分点。六位只增加1个正确样本，同时2～5位
合计净减少4个正确样本，总正确数从234降到231，因此固定预算下的2倍过采样未成功。结果说明六位额外更新
有轻微收益，但通过有放回抽样减少其他长度覆盖率的代价更大；不继续提高该权重。当前共享checkpoint为第19轮
加权T=22模型。
进一步计算表明，有放回权重抽样每轮预计只覆盖约1844张不同图片：每个2～5位长度约339/600张，六位也只
约487/600张；六位1000次抽样中包含大量重复，并没有覆盖全部六位图片。因此该结果否定的是这一采样策略，
尚不能单独否定“增加六位梯度权重”。更干净的后续对照应恢复每轮全部3000张各出现一次，仅在CTC逐样本
loss聚合时给六位乘2，再按权重和归一化，从而保持数据覆盖、batch数量和总计算量不变。

### 六位逐样本CTC loss加权实验

训练入口现支持 `--length-6-loss-weight`，默认1.0与PyTorch标准CTC mean reduction等价。实现先用
`reduction=none` 得到每个样本的CTC loss，再除以各自目标长度保持历史归一化语义，最后按正权重加权并除以
权重和；设为2.0时只有六位样本权重为2，其余为1。训练DataLoader恢复 `length_6_weight=1.0` 的无放回
shuffle，因此每轮完整覆盖全部3000张不同图片；只有反向传播目标被加权，训练集评价、验证集评价、模型选择、
BatchNorm重校准和学习率调度仍使用标准无权重口径。首个对照固定T=22、BiLSTM hidden size 64、20轮和
其余最佳配置，检验六位梯度强调本身能否在不牺牲2～5位数据覆盖的前提下超过76%及总体93.60%。

六位loss权重2实验最佳第18轮：训练准确率99.90%、CER 0.03%，验证准确率94.00%、CER 1.60%，按长度为
96%、100%、96%、94%和84%，泛化差距5.90个百分点。相比未加权T=22基线，总体提高0.40个百分点、CER
降低0.10个百分点、六位提高8个百分点；2/3/4/5/6位分别变化0/+2/-4/-4/+8个百分点。六位多4个正确
样本，3位多1个，而4位和5位各少2个，最终总正确数从234增至235。该结果达到总体与六位双重成功标准，且
明显优于有放回过采样的92.40%/2.10%，确认完整数据覆盖下的目标加权比样本重复更有效。当前共享checkpoint
为第18轮loss加权T=22模型。
错误分析显示15个错误字符串、16次编辑，全部为替换，删除和插入均为0；相比未加权T=22，六位错误从12降到
8、六位编辑距离从12降到8，3次删除全部消失，连续重复字符从33/39提高到36/39并超过V1的34/39。
代价是替换从14增到16，主要体现为少量错误从4～5位重新分配。结果确认loss加权准确击中了六位对齐和重复
字符问题，但剩余瓶颈已转为分散的视觉替换，而非CTC删除。抽查错误图像也支持该判断：`cgca`首字符受到
粗字体和遮挡影响而被判为6，`6p`中的p被干扰线穿过，`23`中的3与圆弧重叠，`nm73`中的n/m细字体相似，
`58a544`中的4被斜线遮挡。错误不再集中于统一序列机制，而是分散于字体、低对比度和干扰线组合。

### 第三阶段阶段性结论

第三阶段已经完成从固定长度分类到可变长度序列识别的学习闭环：CNN把图片转换为22步横向视觉序列，CTC
负责无字符框对齐，BiLSTM提供双向序列上下文，贪心和prefix beam search提供两种解码路径。测试集在全部
模型选择、诊断和实验中保持冻结，当前仍未读取。

本阶段最强配置为V2训练集、T=22、单层双向LSTM（每方向hidden size 64）、六位逐样本CTC loss权重2、
Adam初始学习率0.001、weight decay 0、batch size 16、seed 0、训练集BatchNorm重校准、
`ReduceLROnPlateau`和greedy解码。最佳第18轮验证准确率94.00%、CER 1.60%，按长度为96%、100%、
96%、94%和84%；连续重复字符36/39正确，删除和插入均为0。共享checkpoint文件按项目规则不提交Git。

已确认有效的改动包括BiLSTM序列建模、训练集BatchNorm统计重校准、后期自动降低学习率，以及在完整数据
覆盖下提高六位CTC loss权重。已否定或不再继续的方向包括 `weight_decay=0.0001`、无语言模型beam-10、
把时间步从22增加到45，以及有放回的六位2倍过采样；固定双渲染V2对纯CNN不利，但在BiLSTM下成为当前
训练基底。最终错误已从序列删除和重复字符问题转为15个分散视觉替换，没有单一高频混淆值得继续专项调参。

本项目主要目标是学习模型结构与训练机制，而不是为实际部署持续堆数据刷指标。因此第三阶段到此停止增加
训练集数量和准确率微调；后续学习可在相同数据上进入Transformer Encoder + CTC，对比递归序列建模与
自注意力，但不属于本阶段提交范围。

**阶段状态：第三阶段完成，最佳训练配置与实验结论冻结，测试集继续保留。**

## 第四阶段：CNN + Transformer Encoder + CTC

本阶段沿用第三阶段的数据划分、22 个横向时间步和评估口径，保留 CNN 与 CTC，将 BiLSTM
替换为 Transformer Encoder。目标是跑通并理解自注意力序列识别；验证整串准确率、CER 和
参数量用于观察比较，不要求超过 BiLSTM 的 94.00%。测试集继续封存。

由 AI 逐步实现，再结合代码引导学习：CNN 横向视觉序列 → 位置编码 → Q/K/V 投影、分头、
注意力计算与合头 → 残差连接与 LayerNorm → 前馈网络（FFN）→ 字符分数与 CTC。
注意力使用显式的最小教学实现，线性层与 LayerNorm 使用 PyTorch 组件，不扩展为通用框架。
先检查前向传播，再接 CTC 与小样本过拟合，最后进行正式实验。

### 第一步：图片变成序列

运行最小形状检查（随机输入与随机初始化，不读取数据集或 checkpoint）：

```bash
uv run python -m stage4_transformer_encoder.inspect_sequence
```

复用第三阶段 CNN，数据形状依次为：

```text
[B, 3, 100, 180] → [B, 128, 3, 22] → [B, 64, 3, 22]
                 → [B, 64, 22] → [B, 22, 64]
```

`mean(dim=2)` 只汇聚高度；`transpose(1, 2)` 把通道移到最后一维。
输出中的 22 是从左到右的视觉位置数，不是字符数；64 是每个位置的特征维度。
本步骤尚未加入位置编码、Encoder 或训练，不代表第四阶段模型已经完成。

### 第二步：位置编码

`stage4_transformer_encoder/position_encoding.py` 使用固定正弦/余弦位置编码，不引入可训练参数。
对横向位置 `p` 和特征维度对 `2i, 2i+1`：

```text
PE[p, 2i]   = sin(p / 10000^(2i/D))
PE[p, 2i+1] = cos(p / 10000^(2i/D))
输出 = CNN 横向视觉序列 + PE
```

每个位置获得一个 D=64 维向量，不同维度对使用不同频率。同一位置在不同图片中使用相同编码。
编码通过逐元素相加融入视觉特征，不是拼接，也不是新增字符类别；输出仍为 `[B, 22, 64]`。
这是位置线索，不是位置间的关联计算；后者将在自注意力中学习。

运行 `uv run python -m stage4_transformer_encoder.position_encoding` 检查位置值、形状与梯度。
第一步的 `inspect_sequence` 命令现在也显示加入位置编码后的形状；仍未训练或读取 checkpoint。

### 第三步：Q/K/V 投影

`inspect_sequence` 现在继续对加入位置编码的序列应用三套独立的 `nn.Linear(64, 64)`：

```python
query = q_projection(positioned)
key = k_projection(positioned)
value = v_projection(positioned)
```

三者均为 `[B, 22, 64]`。Q（Query）与 K（Key）将用于计算位置之间的匹配分数，
V（Value）是之后按注意力权重汇总的信息。它们来自同一序列，因此这里准备的是自注意力。
每套投影参数在所有位置间共享，但 Q/K/V 三套参数相互独立；投影仅作用于最后一个维度，
本步骤尚未混合不同位置的信息。与固定位置编码不同，这些投影参数需要在后续训练中学习。

仍运行 `uv run python -m stage4_transformer_encoder.inspect_sequence`，检查形状、
逐位置投影一致性和三套投影的梯度。此处使用的平方均值仅用于反向传播检查，不是识别任务的
训练目标；未更新参数，未计算注意力权重，也未进行多头拆分。

### 第四步：单头自注意力

在 Q/K/V 后加入三步计算：

```python
scores = query @ key.transpose(-2, -1) / (query.size(-1) ** 0.5)
weights = scores.softmax(dim=-1)
attended = weights @ value
```

`scores` 和 `weights` 均为 `[B, 22, 22]`：每行对应一个查询位置，每列对应一个被参考位置。
点积衡量 Q/K 的匹配程度；除以 `sqrt(64)=8` 用于缓解维度增加造成的分数尺度增大与 softmax
过度饱和。softmax 沿最后一维把每行分数转为非负、总和为 1 的权重。
输出为 `[B, 22, 64]`，每个位置用自己的一行权重汇总同一张图片的全部 V 向量。
这是首次显式混合不同序列位置的信息；CNN 本身此前已通过卷积感受野聚合空间邻域。

当前固定尺寸输入不需要补齐 mask，双向 Encoder 也不使用遮挡右侧位置的因果 mask。
权重来自随机初始化模型，尚无学到的识别意义。`inspect_sequence` 现检查权重行和、
加权汇总、与 PyTorch 内置注意力的一致性，以及注意力输出到 Q/K/V 投影的反向传播；
平方均值仍仅用于梯度检查，未更新参数，尚未加入多头、输出投影、残差或 LayerNorm。

### 第五步：多头注意力

在保留单头示例的同时，`inspect_sequence` 添加 4 头教学版本。Q/K/V 各自从 `[B, 22, 64]`
变为 `[B, 4, 22, 16]`：拆的是投影后的特征维度，不是序列长度，每个头仍可参考全部 22 个位置。
Q/K/V 投影的每个输出维度都可以使用完整的 64 维输入，因此这不是把原始输入硬分给不同头。

各头独立计算注意力，分数除以每头维度的平方根 `sqrt(16)=4`，权重形状为 `[B, 4, 22, 22]`。
将各头的 16 维输出拼回 64 维，再经可训练的 `Linear(64, 64)` 输出投影混合各头信息。
多头不是复制四份相同结果，也不是对四个头求平均；各头可能学到不同关联，但不预先指定其职责。

运行同一命令检查每个头的权重行和、内置注意力对照、分头/合头顺序，以及输出投影到 Q/K/V 的
梯度。仍未训练；下一步加入残差连接与 LayerNorm。

### 第六步：残差连接与 LayerNorm

当前教学版本采用 Post-LN（先残差相加，再归一化），沿着已完成的数据流继续添加：

```python
residual = positioned + multihead_output
layer_norm = nn.LayerNorm(64)
normalized = layer_norm(residual)
```

残差把注意力子层输入直接加回输出，让子层可以学习对输入的修正，并提供直接的梯度路径。
这是逐元素相加而非拼接，两侧都为 `[B, 22, 64]`。注意力输出为零时，残差相加结果等于输入，
但随后的 LayerNorm 仍会变换它；不能把整个 Post-LN 子层说成恒等映射。

`LayerNorm(64)` 对每张图片、每个位置各自的 64 个特征计算均值与方差，先标准化，
再应用可训练的逐维缩放与偏移。它不跨图片或位置统计，不使用 BatchNorm 的运行均值/方差；
训练和推理使用相同的归一化计算。由于 epsilon 及可训练的缩放/偏移，最终结果不保证始终
严格满足均值 0、方差 1。输出形状保持不变。

`inspect_sequence` 检查残差结果、LayerNorm 手算公式及单位置独立计算的一致性，
并通过随机目标的均方误差验证梯度能传回 LayerNorm、输出投影、Q/K/V 和序列输入。
该误差仅用于检查，CNN 不参与反向传播，未更新参数；下一步再加入 FFN，形成完整 Encoder 层。

### 第七步：FFN 与完整 Encoder 层

FFN（前馈网络）接收第一组残差与 LayerNorm 的输出，对每个位置分别执行
`Linear(64, 128) → ReLU → Linear(128, 64)`。128 是本教学版本选取的中间维度，
不是位置数，也不是必须使用的固定倍数。ReLU 引入非线性；如果没有非线性，两个线性层
（含偏置）的组合仍可合并成一个线性层。

所有位置共享同一套 FFN 参数，但 FFN 不直接混合不同位置；它处理的是已经由注意力汇总的
上下文特征。输出恢复到 64 维后，加回 FFN 自己的输入，再经过第二个独立的 LayerNorm：

```text
x → 多头注意力 → 加回 x → LayerNorm₁ → y
y → FFN        → 加回 y → LayerNorm₂ → Encoder 输出
```

至此形成一层不含 dropout 的教学版 Post-LN Encoder，输入与输出均为 `[B, 22, 64]`。
输出仍是特征，不是字符概率。`inspect_sequence` 检查 FFN 形状、单位置独立计算、第二组
残差与归一化，以及从最终输出到全部 Encoder 参数和输入序列的梯度；仍不训练或读取权重。
下一步接字符分类头与 CTC。

### 第八步：字符分类头与 CTC

用 `Linear(64, len(characters) + 1)` 把 Encoder 输出转换为 `[B, 22, 21]` 的 logits：
每个时间步有 20 个字符类别和一个 blank 类别。21 不是字符串长度，logits 也尚不是概率。
复用第三阶段 `encode_ctc_targets` 和 `compute_ctc_loss`；后者内部执行 `log_softmax`
并转换为 PyTorch CTC 所需的 `[T, B, C]`，不需要在调用前额外执行 softmax。

示例标签 `a7` 和 `aa2` 编码为拼接的一维目标与长度 `[2, 3]`，无需指定每个字符对应哪个时间步。
CTC 损失汇总能折叠成目标标签的有效路径概率，不是先贪心解码再比较文本。
路径折叠规则为先合并相邻重复类别，再删除 blank：`a,a → a`，`a,blank,a → aa`。
blank 是额外的输出类别，不是真实标签字符，也不是空格或结束符。

`inspect_sequence` 现用随机图片和上述示例标签检查 logits、目标编码、重复字符折叠、有限的
CTC 损失及分类头到 Encoder 输入的梯度。标签不对应随机图片的真实内容，打印的损失仅用于
接口检查，不能视为学习效果；不更新参数，CNN 仍不参与反向传播，未读取数据集或 checkpoint。

### 第九步：可训练模型与极小训练集过拟合

`stage4_transformer_encoder/models.py` 将上述计算整理为 `EncoderLayer` 与 `CaptchaTransformer`。
仍使用显式 Q/K/V 和注意力计算，复用第三阶段 CNN 结构与分类头结构，不加载旧权重。
模型为一层 Post-LN Encoder、4 头、64 维特征、128 维 ReLU FFN、无 dropout，总参数量 141,141。
`inspect_sequence` 现在直接引用模型组件，并校验逐步计算与模型 forward 一致，避免教学与训练实现脱节。

```bash
uv run python -m stage4_transformer_encoder.inspect_sequence
uv run python -m stage4_transformer_encoder.overfit_tiny
```

过拟合检查只读取 `data/stage3_variable_length_v2/train`，按清单顺序从 2～6 位各取前两张，
索引为 `[4,5,2,3,0,1,8,9,12,13]`。这 10 张图片对应 5 个标签的双渲染版本：
`m5`、`bf3`、`x6yp`、`82ffp`、`b36me3`，并非 10 个不同标签。
CPU、4 线程、seed 0、Adam lr=0.001、batch size 10，默认最多 500 轮，全 CNN 与 Encoder
参与反向传播；每轮以 `model.eval()` 在同一批图片上检查整串准确率，达到 100% 即停止。

首轮训练模式 CTC loss 为 16.1703；第 75 轮 loss 为 0.3257、推理模式识别正确 7/10；
第 96 轮 loss 为 0.1360、推理模式正确 10/10，检查通过。包含重复字符的 `82ffp` 同样正确。
这只证明模型能记住极小样本，不能证明泛化能力或超过 BiLSTM。未读取验证集/测试集，未保存权重，
未启动正式训练。后续实验继续沿用第三阶段数据划分与评估口径。

### 第十步：正式训练入口（由学习者运行）

训练命令由学习者亲自运行；AI 只准备代码、执行非训练检查并解读输出，不自行启动训练。

```bash
uv run python -m stage4_transformer_encoder.train --epochs 20
```

默认 `--device auto`，按 CUDA → Apple MPS → CPU 的顺序选择可用设备；可显式指定设备。
CPU 线程数默认 4，batch size 16、Adam lr=0.001、weight decay=0、seed=0。
从随机初始化开始，每轮无放回地遍历 V2 训练集；六位标签的逐样本 CTC loss 权重为 2。
只使用 `data/stage3_variable_length_v2/train` 与原有 `data/stage3_variable_length/validation`，
V2 仅替换训练数据，不另建验证集，也不读取测试集。

沿用第三阶段的训练损失、贪心解码、整串准确率、CER、按长度指标和最佳模型选择逻辑。
每轮在模型副本上用完整训练集重校准 BatchNorm，然后以同一个副本评价训练集和验证集；
优化器引用的原模型保持自己的运行统计，不被校准副本覆盖。校准后验证 loss（无六位加权）
驱动 `ReduceLROnPlateau`，factor=0.3、patience=1、min_lr=1e-5。

最佳模型优先比较验证整串准确率，再比较 CER，最后比较验证 loss。
仅保存到本阶段共享的 `stage4_transformer_encoder/checkpoints/model.pth`，包含校准后的模型状态、
结构与训练配置、最佳轮次和验证指标；原子替换旧文件，不影响第三阶段 checkpoint。
该目录被 Git 忽略。再次运行会重新初始化并覆盖本阶段最佳权重，不是断点续训；
未保存优化器与调度器状态，不支持 `--resume`。

准备阶段只运行 `--help` 与 `test_train_wiring` 等非训练检查。后者使用合成数据、替身训练步骤，
禁止调用 `Adam.step`，检查 BN 副本隔离、参数校验与 checkpoint 保存/严格加载；不读取真实
验证集、不保留临时权重。正式训练效果等待学习者运行后记录。

### 首次正式训练与验证错误分析

学习者在 MPS 上运行 20 轮（batch=16、lr=0.001、seed=0，其余参数沿用上述默认配置）。
最佳为第 19 轮：训练整串准确率 100%、CER 0%，验证整串准确率 90.80%、CER 2.50%、
loss 0.085333，准确率泛化差距 9.20 个百分点。验证 2～6 位准确率依次为
96%、94%、98%、94%、72%。第 15 轮后学习率降至 0.0003；第 20 轮准确率与 CER 未变，
但 loss 略高，未替换第 19 轮权重。

相较第三阶段最佳 BiLSTM（175,573 参数、验证 94.00% / CER 1.60%、六位 84%），
当前 Transformer 为 141,141 参数（少约 19.6%），验证准确率低 3.20 个百分点，
CER 高 0.90 个百分点，六位低 12 个百分点。这是当前配置的单次实验对照，不能推广为架构优劣结论。

只做推理的错误分析命令：

```bash
uv run python -m stage4_transformer_encoder.analyze_validation
```

分析复用第三阶段的 Levenshtein 对齐、统计与报告逻辑，默认自动选设备；未训练、未改权重、
未读取测试集。MPS 复核第 19 轮 checkpoint 得到 227/250 正确，与训练日志一致。
23 个错误字符串合计 25 次字符编辑：替换 23、删除 2、插入 0；其中 21 个字符串只含替换，
2 个只含删除。这里的操作统计来自一条最短编辑对齐，不等于模型内部的因果解释。

六位错 14/50，占全部错误字符串约 60.9%；连续重复字符样本正确 33/39（84.62%），
其 6 个错误中只有 `58a544 → 58a54` 涉及删除，其余均为替换。
另一处删除为 `y5 → 5`，因此不能把所有错误归因于连续重复折叠。
最常见混淆是 `n→m` 3 次、`m→n` 2 次，其余替换对各 1 次。
主要现象是字符替换而非大量漏字；具体是否由图像干扰、特征提取或序列建模导致，仍需进一步检查。

本地报告为 `stage4_transformer_encoder/evaluation_reports/validation_summary.json` 与
`validation_errors.csv`，该目录不提交 Git。

### 验证错误图片抽查

对上述错误明细中的 11 张原图做人工视觉抽查，未改图、未重训：
`nmp`、`nm73`、`4fnnap`、`am`、`47m5be`、`y5`、`58a544`、`a2mmy4`、
`fnxxc`、`8wwgnf`、`b7dygd`。

- `nmp → mmp`、`nm73 → mm73`：首字符 n 附近没有明显干扰线遮挡，字符间也有可见间隔。
  `am → an` 的 m 仍可辨认。这说明至少部分 n/m 错误不能仅用线条遮挡或字符粘连解释。
- `47m5be → 47n5be`：m 的字体笔画粗重，局部形状紧密；`4fnnap → 4fmnap` 中字符大小
  不一致。这些是可见的字体/尺度差异，不能仅凭图像确定哪一个网络模块造成误判。
- `y5 → 5`：斜线穿过 y 的下部；`58a544 → 58a54`：绿色斜线经过末尾重复 4 附近。
  两处漏字都有可见干扰，但尚无无干扰对照，不能断言漏字由干扰线或 CTC 折叠单独导致。
- `a2mmy4 → c2mmw4`：弧线紧邻 y，首字符 a 附近却没有同样遮挡；同一张图的两个替换
  不应强行归为同一个原因。`b7dygd → n7dygd` 的首字符 b 与斜线重叠。
- `fnxxc → pwxxc` 与 `8wwgnf → 8wmgnf` 呈现不同的字体粗细和字形，可见形态差异，
  但并非所有错误位置都被干扰线直接穿过。

抽查支持“错误来源不单一”：既有可见干扰，也有相对清晰的字符混淆。结合训练 100% 与验证
90.80% 的差距，可以确认存在泛化差距，但不能据此定位为 CNN、注意力或位置编码的单独缺陷。
本次未增加层数、时间步、数据或解码器；后续若做正则化对照，应固定其余条件并由学习者运行。

### Dropout 正则化对照（待学习者训练）

新增 `--dropout`，默认 0 保持无 Dropout 基线。此次只在注意力输出投影后、FFN 输出后，
各加一次 Dropout，再做残差相加；不改 CNN、位置编码、注意力权重或 FFN 中间激活。
两次调用虽使用同一个 Dropout 模块，但分别随机生成掩码，原输入的直接残差路径不丢弃。

```text
y = LayerNorm₁(x + Dropout(Attention(x)))
z = LayerNorm₂(y + Dropout(FFN(y)))
```

训练时以概率 p 将分支输出的元素置零，其余元素乘以 `1/(1-p)` 保持期望；
不是删除模型参数或整个字符。推理和 BN 校准时 Dropout 关闭，仍输出完整维度。
它不增加参数，模型仍为 141,141 参数。是否改善验证表现由实验决定，不预先保证。

由学习者运行以下命令，除了 Dropout 外保留首轮实验的 MPS、20 轮及其他配置：

```bash
uv run python -m stage4_transformer_encoder.train --epochs 20 --device mps --dropout 0.1
```

该命令从随机初始化开始，将覆盖本阶段共享 checkpoint，不另存按参数命名的 `.pth`。
基线 90.80% / CER 2.50%、六位 72% 已记录在上文。
checkpoint 记录实际 Dropout 概率，分析入口按该配置构建模型；旧 p=0 权重仍可严格加载。
同一个随机种子不保证两组实验的随机数消耗轨迹完全相同，本轮属于单种子配置对照。

非训练检查覆盖 p=0 默认值、非法概率、训练模式随机性、推理模式与 p=0 的一致性、
校准时关闭 Dropout，以及 p=0.1 配置保存/加载。未启动此次对照训练。

### Dropout 对照结果与位置编码消融

学习者运行残差分支 Dropout=0.1、MPS、20 轮后，最佳为第 20 轮：训练准确率 100%、
CER 0%，验证准确率 90.80%、CER 2.70%、loss 0.0864；2～6 位准确率为
96%、96%、96%、90%、76%。相比无 Dropout 的最佳结果，整串正确数仍为 227/250，
字符编辑错误从 25 增至 27，六位多对 2 张、其他长度合计少对 2 张。
本次单种子对照没有显示总体收益，不能据此断言所有 Dropout 配置均无效。

下一次实验恢复 Dropout=0，只关闭显式正弦位置编码，其他配置保持不变：

```bash
uv run python -m stage4_transformer_encoder.train --epochs 20 --dropout 0 --no-position-encoding
```

默认仍开启位置编码；`--no-position-encoding` 才关闭。关闭只跳过将正弦向量加到 CNN 序列的
步骤，不打乱 22 个横向位置，也不删除 CNN 所提取的空间结构，CTC 仍按原时间轴对齐。
因此该实验检验的是显式位置编码的额外作用，不是宣称整个模型完全没有顺序信息。

直接比较无 Dropout、有位置编码基线的验证 90.80% / CER 2.50%、六位 72%。
这是重新初始化并训练，不是直接在已有权重上关掉位置编码做推理。参数量仍为 141,141。
运行后仍覆盖本阶段唯一共享权重。checkpoint 保存 `use_position_encoding`，分析入口按配置
还原；缺少该字段的历史权重按开启处理。非训练检查覆盖开关默认值、关闭分支计算、形状/参数量
以及配置保存/加载；新对照训练由学习者执行。

### 位置编码消融结果与 Pre-LN 对照

学习者完成关闭位置编码、Dropout=0、MPS、20 轮对照。最佳第 18 轮：训练准确率 100%、
CER 0%，验证准确率 90.80%、CER 2.80%、loss 0.1073，2～6 位准确率为
96%、94%、92%、96%、76%。相比有位置编码基线，整串正确数相同，字符编辑错误从 25 增至 28；
当前没有总体收益，后续恢复位置编码。这不代表无位置编码的自注意力能直接感知数组下标：
CNN 空间结构、序列排列及 CTC 时间轴仍然保留。

下一步研究 LayerNorm 的放置位置。默认仍为 Post-LN，新增 `--pre-norm` 切换至 Pre-LN：

```text
Post-LN: y = LN₁(x + Attention(x)); z = LN₂(y + FFN(y))
Pre-LN:  y = x + Attention(LN₁(x)); z = y + FFN(LN₂(y))
```

Pre-LN 对计算分支的输入做归一化，直接残差路径不经过 LayerNorm；注意力的 Q/K/V 都来自
LN₁(x)。本次只移动层内两个 LayerNorm，不额外增加 Encoder 末尾的最终 LayerNorm，
分类头直接接层输出。因此这是单层内部归一化位置对照，不是所有 Pre-LN 网络设计的比较。
参数量仍为 141,141，不承诺训练或验证表现必然改善。

由学习者运行，恢复位置编码、Dropout=0，其余保持不变：

```bash
uv run python -m stage4_transformer_encoder.train --epochs 20 --dropout 0 --pre-norm
```

不加 `--no-position-encoding`；自动选设备时确认仍为 MPS。与有位置编码、无 Dropout 的
Post-LN 基线 90.80% / CER 2.50%、六位 72% 对照，观察 loss 波动、降学习率时点和最佳验证指标。
此命令从头训练并覆盖共享权重；`norm_first` 随 checkpoint 保存，分析入口按配置还原。
非训练检查使用相同参数与 PyTorch `TransformerEncoderLayer` 对照两种前向计算，并验证
计算分支置零后 Pre-LN 残差保留输入，以及配置保存/加载；未启动此次训练。
### 缩小 FFN 容量对照（待学习者训练）

新增 `--ffn-dim`，默认 128 保持原模型；本次只将 FFN 中间维度从 128 改为 64，其他条件恢复基线：
Post-LN、位置编码开启、Dropout=0。

```bash
uv run python -m stage4_transformer_encoder.train --epochs 20 --dropout 0 --ffn-dim 64
```

FFN 由 `64 → 128 → 64` 改为 `64 → 64 → 64`，参数量减少 8,256（141,141 → 132,885）；
注意力、CNN、时间步和 CTC 不变。请比较训练/验证差距、验证 CER 和六位准确率；训练仍由学习者运行。

### 只用 V2 新渲染对照（待学习者训练）

新增 `--new-renders-only`，只保留 V2 manifest 中 `source=new_render` 的 1,500 张训练图，
验证集和模型配置不变。这是诊断对照，不是公平的增量数据实验，因为训练样本量减半。

```bash
uv run python -m stage4_transformer_encoder.train --epochs 20 --dropout 0 --new-renders-only
```

保持 Post-LN、位置编码、FFN=128；请先确认日志显示 `训练=1500`。不预设准确率会提升，
训练仍由学习者运行并覆盖共享 checkpoint。

### 在线视觉多样性：同标签独立重渲染（待学习者训练）

已完成的仿射增强 40 轮对照：seed=1 最佳第 37 轮，验证准确率 95.20%、CER 1.30%、
六位 88%；seed=2 最佳仍为第 16 轮，验证准确率 94.00%、CER 1.70%、六位 80%。
两个种子的均值为准确率 94.60%、CER 1.50%、六位 84%。延长训练并非每个种子都有收益。

新增 `--rerender-probability`（默认 0，关闭）。本次设为 0.5：每次读取训练样本时，
50% 概率保留原图，50% 概率用原标签重新生成图片，然后统一执行 `--augment` 的轻微仿射增强。
复用第三阶段生成器，字体从当前训练记录中出现过的字体去重后均匀抽取；颜色、背景、干扰与
字形扰动重新取样。只在内存生成，不增加标签、不改磁盘数据，每轮仍是 3,000 个样本。
重渲染使用独立的 Python 随机流，当前单进程加载下同种子、同读取顺序可复现；
概率为 0 时不消耗该随机流，保留原有仿射行为。启用时字体缺失会报错，不静默换字体。

验证、原图训练指标和 BN 校准仍读取原图，不重渲染、不仿射；不读取测试集。
因此训练准确率是对固定训练原图的评价，并非当轮所有随机视图的准确率。
本次只检验增加视觉变化是否有收益，不预设一定提高准确率。与上述同种子 40 轮结果比较：

```bash
for seed in 1 2; do
  uv run python -m stage4_transformer_encoder.train \
    --epochs 40 --dropout 0 --augment \
    --rerender-probability 0.5 --seed "$seed" || break
done
```

仍从随机初始化开始，自动选择设备，依次覆盖本阶段唯一共享 checkpoint；记录重渲染概率。
CPU 在线绘图会增加训练耗时，不生成额外 PNG 或分种子权重。训练由学习者运行。
非训练检查命令：`uv run python -m stage4_transformer_encoder.test_train_wiring`，
覆盖开关、混合分支、同种子复现、标签/形状、原数据不变及 BN/验证隔离；禁止优化器更新。

### 保留横向细节：22 → 45 个位置（待学习者训练）

50% 在线重渲染的 40 轮结果：seed=1 最佳第 20 轮，准确率 95.60%、CER 1.20%、六位 84%；
seed=2 最佳第 32 轮，准确率 94.40%、CER 1.40%、六位 82%。两个种子平均准确率 95.00%、
CER 1.30%、六位 83%，相对仅仿射增强是小幅收益，不是六位准确率的稳定提升。
当前 seed=2 权重验证复现为 14 张错误：10 次替换、3 次删除、1 次插入；其中六位 9 张
（7 次替换、2 次删除）。n→m 四次、m→n 一次。提示需要检验字形细节辨别，并不证明下采样是根因。

新增 `--sequence-width 45`，默认仍为 22。复用第三阶段的池化配置：第三次池化从 `(2, 2)`
改成 `(2, 1)`，只减少横向下采样。输入仍为 180×100，特征序列从 `[B,22,64]` 变为
`[B,45,64]`；正弦位置编码按实际长度生成，每个注意力头仍看所有位置，CTC 自动使用输出长度。
参数量仍为 141,141，但注意力矩阵从 22×22 变为 45×45，计算和内存开销增加。
这也改变了横向感受野及 CTC 对齐空间，并非只改变细节清晰度；不预设一定改善。

其余设置保持本轮基线，由学习者运行：

```bash
for seed in 1 2; do
  uv run python -m stage4_transformer_encoder.train \
    --epochs 40 --dropout 0 --augment --rerender-probability 0.5 \
    --sequence-width 45 --seed "$seed" || break
done
```

从头训练并覆盖共享 checkpoint，不直接复用 22 位置权重做对照。权重保存实际 sequence_width，
验证分析入口按配置还原。非训练检查覆盖默认 22、45 位置前向与 CTC loss、参数量不变、
45 位置 checkpoint 保存/加载及验证分析入口；不启动训练、不读取测试集。

### 45 位置结果与解码对照

40 轮、仿射增强、重渲染概率 0.5：seed=1 最佳第 30 轮，验证准确率 96.00%、CER 1.10%、
六位 88%；seed=2 最佳第 39 轮，准确率 96.40%、CER 1.00%、六位 90%。
两种子均值准确率 96.20%、CER 1.05%、六位 89%，优于相同设置的 22 位置结果
（95.00%、1.30%、83%），但仅两个种子、250 张验证图，不作统计显著性结论。

对当前 seed=2、第 39 轮权重做不训练解码对照：

```bash
uv run python -m stage4_transformer_encoder.analyze_validation --decoder beam --beam-width 10
```

beam=10 与 greedy 均为 241/250 正确、CER 1.00%、六位 90%；逐错误 CSV 完全一致，
9 张错图合计 10 次编辑（5 替换、2 删除、3 插入），两个报告使用同一份 checkpoint。
本次没有解码收益，继续保留 greedy；不能推断所有 beam 宽度均无效，也不能仅凭此断定错误根因。
复用第三阶段 prefix beam search，不加词典或长度约束。默认仍 greedy，beam 报告写入
`stage4_transformer_encoder/evaluation_reports/beam_10/`，不覆盖 greedy 报告或权重。
非训练接线检查覆盖 beam 参数传递和报告目录；测试集未读取。

### 第四阶段收尾：已完成

学习者确认当前效果满足本阶段学习目标，结束 CNN + Transformer Encoder + CTC 阶段，
不再追加调参或实验。上文“待学习者训练”标题保留为历史实验计划，结果以后续记录及本节为准。

- 最终采用配置：45 个横向位置、64 维特征、单层四头 Encoder、FFN=128、Post-LN、
  正弦位置编码、Dropout=0；参数量 141,141。
- 训练配置：40 轮、batch=16、初始 lr=0.001、六位 loss 权重=2、轻微仿射增强、
  在线同标签重渲染概率=0.5、BN 原图重校准、自动学习率；推理保留 greedy。
  CLI 默认 sequence_width 仍为 22，复现最终配置需显式传入 `--sequence-width 45`。
- 最终共享权重：seed=2、第 39 轮；验证集 241/250 正确（96.40%），CER=1.00%，
  六位准确率 90%。seed=1/2 最佳验证准确率均值为 96.20%，不是测试集或生产准确率。
- 学习覆盖：CNN 特征转序列、位置编码、Q/K/V、多头注意力、残差连接、LayerNorm、
  Pre/Post-LN、FFN、CTC 对齐与解码，以及控制变量、多种子对照和错例分析。
- 实验结论：本项目中视觉多样性与减少横向下采样获得收益；Dropout、缩小 FFN 等没有
  显示预期的总体收益；当前权重 beam=10 与 greedy 完全一致。以上结论仅适用于已测配置。

保留本阶段唯一共享 checkpoint，不额外复制权重，不提交训练生成的 `.pth`。
本次收尾只更新文档，不训练、不读取测试集；独立测试评估尚未执行。
